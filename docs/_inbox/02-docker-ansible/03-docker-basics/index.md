---
title: 03 Основы Docker
---

Архитектура Docker, контейнеры и базовые команды управления.

---

## 📚 Содержание

#### **[[01-containers-intro|01 Docker контейнеры - введение]]**

**Архитектура Docker:**
- Что такое Docker и контейнеры
- Контейнеры vs виртуальные машины
- Docker на Linux (cgroups, namespaces, chroot)
- Процессы в Linux (fork, execv)
- Компоненты Docker (Engine, CLI, Images, Containers)

**Преимущества:**
- Быстрая доставка приложений
- Высокая производительность
- Изоляция зависимостей
- Простое масштабирование

**Результат:** Понимание как работает Docker изнутри.

---

#### **[[02-container-lifecycle|02 Жизненный цикл контейнера]]**

**Управление контейнерами:**
- Жизненный цикл контейнера (Created → Running → Stopped)
- Основные команды: `docker run`, `docker create`, `docker start`, `docker stop`
- Остановка graceful vs принудительная (stop vs kill)
- Перезапуск, пауза, возобновление (restart, pause, unpause)
- Удаление контейнеров (rm, prune)

**Важные концепции:**
- Stateful контейнеры — состояние сохраняется между запусками
- `docker ps` и `docker ps -a` для просмотра
- Переименование, форматирование вывода
- Практический пример с MongoDB

**Результат:** Полное управление жизненным циклом контейнеров.

---

#### **[[03-logs-debugging|03 Логи и отладка контейнеров]]**

**Мониторинг контейнеров:**
- `docker stats` — CPU, RAM, сетевой трафик
- `docker inspect` — JSON информация с фильтрацией
- Практические примеры извлечения данных

**Работа с логами:**
- `docker logs` — просмотр логов
- `docker logs -f` — follow mode (в реальном времени)
- Фильтрация с grep: ERROR, WARNING, и т.д.
- Сохранение логов в файл

**Выполнение команд в контейнере:**
- `docker exec` — запуск команд в работающем контейнере
- Параметры: `-i`, `-t`, `-it`, `-d`, `-e`, `-u`, `-w`
- Интерактивная shell: `docker exec -it <name> bash`
- Отладка: ps, netstat, df, top, и т.д.

**Практическое упражнение:**
- Создание MongoDB контейнера
- Проверка сохранности состояния
- Интерактивное взаимодействие

**Результат:** Полная отладка и мониторинг контейнеров.

---

## ✅ Checklist

**После [[01-containers-intro|01 Docker контейнеры - введение]]:**
- ✅ Понимаешь как работает Docker
- ✅ Знаешь разницу между контейнерами и VM
- ✅ Знаешь компоненты Docker

**После [[02-container-lifecycle|02 Жизненный цикл контейнера]]:**
- ✅ Управляешь жизненным циклом контейнера
- ✅ Знаешь все команды (run, create, start, stop, kill, rm)
- ✅ Понимаешь Stateful контейнеры

**После [[03-logs-debugging|03 Логи и отладка контейнеров]]:**
- ✅ Мониторишь ресурсы контейнера
- ✅ Читаешь логи и фильтруешь их
- ✅ Выполняешь команды в контейнере
- ✅ Отлаживаешь проблемы контейнеров

---

## 🔗 Связи

**Предыдущий раздел:**
- [[docs/_inbox/02-docker-ansible/02-environment/index|02 Подготовка окружения]]

**Следующий раздел:**
- [[docs/_inbox/02-docker-ansible/04-docker-images-dockerfile/index|04 Образы и Dockerfile]]

---
