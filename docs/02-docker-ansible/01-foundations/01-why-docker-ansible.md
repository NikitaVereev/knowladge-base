---
title: Docker и Ansible - для чего нужны
---

Понимание проблем и решений в современной разработке и инфраструктуре.

---

## 📋 История проблемы

#### Раньше (FTP и ручные развертывания)

- Разработчик пишет код локально
- Отправляет файлы по FTP на сервер
- Надеется, что среда на сервере совпадает с локальной
- Результат: "У меня работает, но на сервере ошибка"

#### С Git (чуть лучше)

- Git решил проблему версионирования кода
- Но появились новые проблемы:
  - **Конфликты зависимостей:** Приложение А использует Python 3.8, приложение B - Python 3.11
  - **Отсутствие изоляции:** Приложения конкурируют за порты, файлы, память
  - **Несовместимость сред:** Код работает на macOS разработчика, но падает на Linux сервера
  - **Масштабирование:** Как запустить одно приложение на 100 серверах?

---

## 🐳 Решение: Docker

**Docker** упаковывает приложение со всеми зависимостями в контейнер.

#### Преимущества Docker

**Изоляция и консистентность**
- Каждый контейнер изолирован от других
- Работает одинаково на любой машине (локально, на сервере, в облаке)

**Быстрые обновления**
- Новая версия приложения = новый Docker образ
- Откат на старую версию в одну команду

**Масштабирование**
- Запустить 100 копий приложения одной командой
- Управление кластером контейнеров просто

**Простая доставка**
- Все зависимости в одном образе
- Не нужно устанавливать 20 пакетов на сервер

**Управление сетью**
- Контейнеры общаются между собой просто
- Открыть порты и роутинг встроены

---

## 🤖 Улучшение: Ansible

Docker решает проблему **как запустить приложение**. Но что с:
- Установкой Docker на 50 серверов?
- Обновлением конфигурации на всех машинах?
- Развертыванием 100 контейнеров?
- Резервными копиями?

**Ansible** автоматизирует всё это.

#### Преимущества Ansible

**Автоматизация**
- Повторяющиеся задачи выполняются автоматически
- Одна команда на множество серверов

**Конфигурация как код**
- Инфраструктура описана в YAML файлах
- Можно версионировать её в Git
- Воспроизводить на других машинах

**Экономия времени**
- Развертывание с часов на минуты
- Обновление 1000 серверов в секунды

**Портативность**
- Описание инфраструктуры переносится между проектами
- Миграция между облаками просто

**Декларативное описание**
- Ты описываешь **какое состояние хочешь**, не **как это сделать**
- Ansible сам разбирается как этого достичь

---

## 🎯 Docker + Ansible = мощная комбинация

| Проблема | Docker | Ansible |
|----------|--------|---------|
| Зависимости конфликтуют | ✅ Контейнеры изолированы | ✅ Управляет окружением |
| Несовместимость сред | ✅ Работает везде | ✅ Гарантирует состояние |
| Масштабирование | ✅ N контейнеров просто | ✅ N серверов просто |
| Быстрый откат | ✅ Смена образа | ✅ Redeploy playbook |
| Управление инфраструктурой | ❌ Только контейнеры | ✅ Всё автоматизируется |

---

## 🚀 Практический пример

#### Без Docker и Ansible
```
1. SSH на каждый из 50 серверов
2. Установить Python, Node.js, PostgreSQL...
3. Скопировать код приложения
4. Запустить приложение
5. Настроить nginx
6. Молиться что ничего не сломалось
Время: часы/дни
```

#### С Docker и Ansible
```
1. Написать Dockerfile (один раз)
2. Написать Ansible playbook (один раз)
3. Выполнить: ansible-playbook deploy.yml
Время: минуты
```

---

## 📖 Какие инструменты используются

- **Docker** — контейнеризация приложений
- **Docker Compose** — управление несколькими контейнерами
- **Docker Registry** — хранилище образов
- **Ansible** — автоматизация инфраструктуры
- **Vagrant** — управление VM для разработки (опционально)

---

## 🔗 Связи

**Следующий раздел:**
- [[02-docker-ansible/02-environment/index|02 Подготовка окружения]]