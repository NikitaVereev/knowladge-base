---
title: 07 Ansible Fundamentals
---

Основы Ansible: архитектура, установка, инвентарь, модули и автоматизация IT.

---

## 📚 Содержание

#### **[[01-intro-architecture|01 Введение и Архитектура]]**

**Основы:**
- Infrastructure as Code подход
- Проблемы ручного управления серверами
- Ansible vs Chef/Puppet/Terraform сравнение

**Архитектура и компоненты:**
- Control Node и Managed Nodes
- Push модель управления
- Идемпотентность и безопасность
- Жизненный цикл playbook'а
- SSH протокол и Python модули

**Требования:**
- Python 3.12+ (control node)
- Python 3.9+ (managed nodes)
- SSH доступ на порту 22

---

#### **[[docs/02-docker-ansible/07-ansible-foundamentals/02-installation|02 Установка и Конфигурация]]**

**Установка на разные ОС:**
- Arch Linux (pacman, pip, AUR)
- Ubuntu 24.04 (apt, pip, ppa)
- macOS (brew, pip)
- Windows (WSL2, Docker)

**SSH ключи и аутентификация:**
- Генерация ключей (ed25519, RSA)
- ssh-copy-id и проверка подключения
- SSH Agent для защищенных ключей
- Параметры подключения

**Проверка установки:**
- ansible --version
- ansible-doc команды
- Дополнительные коллекции

---

#### **[[03-inventory|03 Инвентарь и Хосты]]**

**Форматы инвентаря:**
- INI формат (простой и логичный)
- YAML формат (гибкий и мощный)
- Группы хостов и вложенность
- Переменные для хостов и групп

**Параметры подключения:**
- ansible_host, ansible_port
- ansible_user, ansible_ssh_private_key_file
- ansible_become и ansible_become_method
- ansible_python_interpreter

**Практические примеры:**
- Простая конфигурация с группами
- Параметры для разных платформ
- Использование переменных в инвентаре
- Тестирование подключения

---

#### **[[04-modules-execution|04 Модули и Выполнение]]**

**Встроенные модули:**
- Пакеты (apt, dnf, yum)
- Сервисы (systemd, service)
- Файлы (copy, template, file)
- Команды (shell, command, script)
- Пользователи (user, group, authorized_key)
- Загрузки (get_url, git, unarchive)

**Ad-hoc команды:**
- Синтаксис и основные параметры
- Примеры (ping, setup, shell, apt)
- Параметры (-b, -u, -K, -v, -l)
- Регистрирование результатов

**Когда использовать:**
- Ad-hoc: быстрые проверки и исправления
- Playbooks: повторяющиеся и сложные сценарии

---

#### **[[05-best-practices|05 Best Practices и Примеры]]**

**Организация проекта:**
- Структура директорий
- Имена переменных и хостов
- Документирование playbooks

**Примеры готовых сценариев:**
- Установка Nginx + PHP + MySQL
- Deploy Node.js приложения
- Настройка брандмауэра и SSH
- Регулярные обновления системы

**Обработка ошибок:**
- Проверка перед выполнением
- Откат при ошибках
- Логирование результатов

---

## 🔗 Структура раздела

```
07-Ansible Fundamentals (этот файл)
├── 01 Введение и Архитектура
├── 02 Установка и Конфигурация
├── 03 Инвентарь и Хосты
├── 04 Модули и Выполнение
└── 05 Best Practices и Примеры
```

---

## 🎯 Ключевые Концепции

**Push vs Pull:**
- Ansible использует Push (управление с одной машины)
- Более простая настройка, но требует доступа

**Идемпотентность:**
- Модули проверяют текущее состояние
- Безопасно запускать многократно
- Предсказуемые результаты

**Agentless архитектура:**
- Не требует установки на целевые серверы
- SSH подключение по умолчанию везде
- Меньше уязвимостей и проблем

---

## ✅ Checklist

**После изучения раздела:**
- ✅ Понимаешь что такое Ansible и когда использовать
- ✅ Ansible установлен и проверен
- ✅ Целевые серверы доступны по SSH
- ✅ Создан и протестирован инвентарь
- ✅ Можешь использовать основные модули
- ✅ Выполняешь ad-hoc команды
- ✅ Готов к написанию playbooks

---
