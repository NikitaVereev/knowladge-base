### Docker Storage and Volumes

Хранение данных в контейнерах Docker.

---

#### 📚 Содержание

##### **[[01-volumes-mounts]]**

**Docker Volumes:**
- Типы хранилища (named volumes, bind mounts, tmpfs)
- Зачем нужны volumes (persistence, sharing, logging)
- Команды управления (create, ls, inspect, rm, prune)

**Named Volumes:**
- Рекомендуемый способ хранения данных
- Создание и управление именованных томов
- Монтирование в контейнеры (`-v`, `--mount`)
- Sharing volumes между контейнерами
- Backup и restore данных

**Bind Mounts:**
- Прямое монтирование директорий хоста
- read-only и read-write режимы
- Для development (синхронизация кода)
- Для production (каталоги конфигурации)
- Отличия от named volumes

**Tmpfs Mounts:**
- Временное хранилище в памяти (OЗУ)
- Когда использовать (sensitive data, secrets)
- Не сохраняется между перезапусками

**VOLUME инструкция в Dockerfile:**
- Объявление mount point'ов в Dockerfile
- Anonymous vs named volumes
- Практические примеры

**Копирование данных (docker cp):**
- `docker cp` для копирования без volumes
- Синтаксис и примеры
- Когда использовать (временные задачи)

**Практические примеры:**
- PostgreSQL с named volume
- MongoDB с backup/restore
- Development setup с bind mounts
- Multi-container databases
- Extraction логов через docker cp

---

#### ✅ Checklist

**После изучения этого раздела:**
- ✅ Понимаешь типы хранилища в Docker
- ✅ Можешь создавать и управлять volumes
- ✅ Можешь монтировать директории хоста (bind mounts)
- ✅ Можешь делать backup и restore данных
- ✅ Знаешь когда использовать каждый тип
- ✅ Можешь использовать docker cp для копирования
- ✅ Понимаешь tmpfs для временного хранилища