---
created: 2026-01-03
tags: [linux, system-basics, system-administration, reference]
type: reference
---

# Linux System Basics - основы Linux системы

## Основная идея

Linux система состоит из нескольких слоёв, и понимание этих основ - фундамент для всей работы.

**Архитектура Linux:**
```
┌─────────────────────────────────┐
│   Приложения (firefox, code)    │
├─────────────────────────────────┤
│   Системные библиотеки (glibc)  │
├─────────────────────────────────┤
│   Linux Kernel (ядро)           │
├─────────────────────────────────┤
│   Аппаратное обеспечение (CPU)  │
└─────────────────────────────────┘
```

**Три основы Linux:**
1. **Файловая система** - как хранятся файлы
2. **Процессы и сигналы** - как запускаются программы
3. **Права доступа** - кто имеет доступ к чему

---

## ЧАСТЬ 1: Файловая система Linux

### Структура директорий (FHS - Filesystem Hierarchy Standard)

```
/                           # Root директория (корень)
├── /bin                    # Основные команды (ls, cat, grep)
├── /sbin                   # Системные команды для root (shutdown, reboot)
├── /usr/                   # Пользовательские программы
│   ├── /usr/bin            # Команды (firefox, vim, gcc)
│   ├── /usr/sbin           # Системные команды
│   ├── /usr/lib            # Библиотеки
│   └── /usr/share          # Данные (документация, шрифты)
├── /etc/                   # Конфигурационные файлы (системные)
├── /home/                  # Домашние директории пользователей
│   └── /home/user/         # Твоя папка
├── /root/                  # Домашняя директория root пользователя
├── /var/                   # Переменные данные
│   ├── /var/log            # Логи (system.log, messages)
│   ├── /var/cache          # Кэш
│   └── /var/spool          # Очереди печати и почты
├── /tmp/                   # Временные файлы (очищается при перезагрузке)
├── /opt/                   # Дополнительные приложения
├── /lib / /lib64           # Системные библиотеки
├── /dev/                   # Файлы устройств (диски, терминалы)
├── /proc/                  # Информация о процессах (виртуальная FS)
├── /sys/                   # Информация о системе (виртуальная FS)
├── /boot/                  # Файлы загрузки (kernel, initramfs)
├── /mnt/                   # Точки монтирования (временные)
├── /media/                 # Съёмные носители (USB, DVD)
└── /root                   # Домашняя папка root
```

### Навигация по файловой системе

```bash
# ОСНОВНЫЕ КОМАНДЫ

# Текущая директория
pwd                                # Print Working Directory

# Список файлов
ls                                 # Базовый список
ls -la                            # Со скрытыми (-a) и деталями (-l)
ls -lh                            # Human-readable размеры
ls -lS                            # Отсортировано по размеру
ls -lt                            # По времени изменения

# Переход в директорию
cd /path/to/directory             # Абсолютный путь
cd ..                             # На уровень выше
cd ~                              # В домашнюю папку (= /home/user)
cd -                              # В предыдущую папку

# Создание директорий
mkdir dirname                     # Одна папка
mkdir -p parent/child/grandchild  # Иерархия папок

# Удаление
rmdir dirname                     # Только пустые папки
rm -rf dirname                    # Рекурсивно (ОПАСНО!)

# Копирование файлов
cp source.txt dest.txt            # Копировать файл
cp -r source/ dest/               # Копировать папку рекурсивно
cp -v source dest                 # С выводом (verbose)

# Перемещение / переименование
mv old_name.txt new_name.txt      # Переименовать
mv file.txt /other/path/          # Переместить в папку

# Посмотреть содержимое файла
cat file.txt                      # Весь файл
head -n 10 file.txt               # Первые 10 строк
tail -n 10 file.txt               # Последние 10 строк
tail -f file.txt                  # Real-time (follow) для логов
less file.txt                     # Постраничный просмотр (q для выхода)
more file.txt                     # То же но старше

# Поиск в содержимом файла
grep "pattern" file.txt           # Строки содержащие pattern
grep -i "pattern" file.txt        # Case-insensitive
grep -r "pattern" /path/          # Рекурсивный поиск

# Поиск файлов
find /path -name "*.txt"          # По имени файла
find /path -type f -size +10M     # Файлы больше 10MB
find /path -mtime -7              # Изменённые за последние 7 дней
find /path -user username         # Принадлежащие пользователю

# Размер файлов / папок
du -h file.txt                    # Размер файла
du -sh dirname                    # Размер папки
du -sh /* | sort -h | tail        # 10 самых больших в корне

# Место на диске
df -h                             # Использование дискового пространства
df -i                             # Inodes (количество файлов)

# Создание пустого файла / обновление времени
touch newfile.txt                 # Создать пустой файл
touch -t 202601031020 file.txt    # Изменить время файла

# Ссылки на файлы
ln -s /path/to/original link      # Символическая ссылка (symlink)
ln /path/to/original hardlink     # Жёсткая ссылка (hardlink)
```

### Типы файлов в Linux

```bash
# ОПРЕДЕЛЕНИЕ ТИПА

# Символ в начале при ls -l указывает тип:
-  rw-r--r--  файл обычный
d  rwxr-xr-x  директория
l  lrwxrwxrwx symlink (символическая ссылка)
c  rw-------  device (символьное устройство)
b  rw-------  device (блочное устройство)
s  srw-------  socket (Unix сокет)
p  prw-------  pipe (именованный канал)

# Определить тип файла
file filename                     # Показывает тип

# Примеры:
file document.pdf                 # PDF document, version 1.4
file archive.tar.gz               # gzip compressed data
file binary                       # ELF 64-bit LSB executable
```

---

## ЧАСТЬ 2: Права доступа (Permissions)

### Запись прав (rwx)

```bash
# Формат при ls -l:
-  rw-r--r--  user  group  size  date  filename
^  ^^^^^^^^^
|  |||||||||
тип права доступа


ПРАВА:  r (read=4)    w (write=2)    x (execute=1)

rw-r--r--  разбирается как:
│  ││││││
│  ││││└─ другие (others): r (4)    = 4
│  │││└── другие: - (0)            = 0
│  ││└─── группа (group): r (4)    = 4
│  │└──── группа: - (0)            = 0
│  └───── владелец (user): w (2)   = 2
└──────── владелец: r (4)          = 4
результат: 644 (user=6, group=4, others=4)
```

### Изменение прав доступа

```bash
# СИМВОЛИЧЕСКИЙ ФОРМАТ

chmod u+x file                    # User: добавить исполнение
chmod g-w file                    # Group: удалить запись
chmod o+r file                    # Others: добавить чтение
chmod a=rx file                   # All: установить r,x (удалить w)

# ЧИСЛОВОЙ ФОРМАТ (октальный)

chmod 755 file                    # rwxr-xr-x (исполняемый файл)
chmod 644 file                    # rw-r--r-- (обычный файл)
chmod 600 file                    # rw------- (приватный файл)
chmod 777 file                    # rwxrwxrwx (все права, ОПАСНО!)
chmod 000 file                    # --------- (без прав, может быть не видно)

# РЕКУРСИВНО

chmod -R 755 dirname              # Применить к папке и всему в ней

# СПЕЦИАЛЬНЫЕ БИТЫ

chmod u+s file                    # SetUID (выполняется от владельца)
chmod g+s dirname                 # SetGID (новые файлы наследуют group)
chmod +t dirname                  # Sticky bit (только владелец может удалить)

# Пример:
# SetUID на /usr/bin/sudo - позволяет обычному пользователю запустить как root
ls -l /usr/bin/sudo               # -rwsr-xr-x (видно 's' вместо 'x')
```

### Владельцы файлов (ownership)

```bash
# Посмотреть владельца
ls -l file.txt                    # user:group видно в выводе

# Изменить владельца
sudo chown username file.txt      # Изменить user
sudo chown username:groupname file.txt  # User и group
sudo chown :groupname file.txt    # Только group

# РЕКУРСИВНО
sudo chown -R username:groupname dirname

# Изменить только группу
sudo chgrp groupname file.txt
sudo chgrp -R groupname dirname

# Практический пример:
# Файл создан root, нужно передать пользователю
sudo chown -R user:user /home/user/documents
```

### Примеры типичных прав

```bash
# Обычный текстовый файл
chmod 644 document.txt            # rw-r--r-- (владелец пишет, остальные читают)

# Исполняемый скрипт
chmod 755 script.sh               # rwxr-xr-x (все могут исполнять)

# Приватный конфиг
chmod 600 ~/.ssh/config           # rw------- (только владелец)

# SSH ключ (ВАЖНО!)
chmod 600 ~/.ssh/id_rsa           # rw------- (ТОЛЬКО владелец!)
chmod 644 ~/.ssh/id_rsa.pub       # rw-r--r-- (публичный, можно читать)

# Директория с файлами
chmod 755 dirname                 # rwxr-xr-x (заходить и читать может каждый)

# Приватная папка
chmod 700 ~/.ssh                  # rwx------ (только владелец)

# Shared папка для группы
chmod 770 shared_dir              # rwxrwx--- (user и group всё, others ничего)
chmod g+s shared_dir              # SetGID - новые файлы будут в группе
```

---

## ЧАСТЬ 3: Процессы и управление ими

### Просмотр процессов

```bash
# ОСНОВНЫЕ КОМАНДЫ

ps aux                            # Список всех процессов
ps aux | grep firefox             # Найти конкретный процесс

# ФОРМАТ ps aux:
# USER    PID  %CPU %MEM   VSZ   RSS TTY STAT START TIME COMMAND
# root      1   0.0  0.1 100000 5000  ?  S    10:00 0:01 /sbin/init
# ↑        ↑   ↑    ↑    ↑     ↑   ↑  ↑   ↑     ↑   ↑
# юзер     PID% эффектив память виртуальная реальная TTY статус

ps -ef                           # Другой формат (дерево processes)
ps -ef --forest                  # Дерево процессов (родитель-потомок)

# Real-time мониторинг
top                              # Интерактивный просмотр (q для выхода)
htop                             # Красивее чем top (требует установки)

# Сортировка в top/htop:
# P = по CPU
# M = по памяти
# T = по времени
```

### Управление процессами

```bash
# ЗАПУСК ПРОЦЕССА

# Обычный запуск
./script.sh                       # В foreground

# Запуск в background
./script.sh &                     # & в конце

# Запуск с нижним приоритетом
nice -n 10 ./script.sh            # Ниже приоритет (медленнее)
nice -n -10 ./script.sh           # Выше приоритет (требует root)

# УПРАВЛЕНИЕ РАБОТАЮЩИМ ПРОЦЕССОМ

Ctrl+C                            # Отправить SIGTERM (остановить)
Ctrl+Z                            # Отправить SIGSTOP (приостановить)

# КОМАНДЫ ДЛЯ BACKGROUND ПРОЦЕССОВ

jobs                              # Список background процессов
fg %1                             # Вернуть процесс #1 в foreground
bg %1                             # Продолжить процесс #1 в background
%1 &                              # Перестартовать процесс в background

# ЗАВЕРШЕНИЕ ПРОЦЕССА

kill PID                          # Послать SIGTERM (graceful shutdown)
kill -9 PID                       # Послать SIGKILL (force kill)
kill -HUP PID                     # SIGHUP (перезагрузить конфиг)
killall firefox                   # Убить все процессы с этим имением

# Сигналы:
# SIGTERM (15) = graceful shutdown (по-умолчанию)
# SIGKILL (9)  = force kill (невозможно игнорировать)
# SIGHUP (1)   = перезагрузить (часто для сервисов)
# SIGSTOP (19) = pause
# SIGCONT (18) = continue
```

### Приоритет процессов (Nice/Renice)

```bash
# ПОСМОТРЕТЬ ПРИОРИТЕТ

ps -l | grep firefox              # Колонка NI = nice value

# ИЗМЕНИТЬ ПРИОРИТЕТ

nice -n 10 command                # Запустить с приоритетом 10
renice 10 -p PID                  # Изменить приоритет существующего процесса
renice -5 -p PID                  # Увеличить приоритет (требует root)

# Nice значения:
# -20 = максимальный приоритет (только root)
# 0   = нормальный приоритет (по-умолчанию)
# 19  = минимальный приоритет
```

### Сигналы процессам (signals)

```bash
# ОСНОВНЫЕ СИГНАЛЫ

SIGHUP (1)    # Hangup - перезагрузить конфиг
SIGINT (2)    # Interrupt - Ctrl+C
SIGQUIT (3)   # Quit - Ctrl+\
SIGKILL (9)   # Kill - force terminate (невозможно игнорировать)
SIGTERM (15)  # Terminate - graceful shutdown
SIGCONT (18)  # Continue - продолжить stopped процесс
SIGSTOP (19)  # Stop - pause (невозможно игнорировать)

# ПРИМЕРЫ

kill -HUP PID                     # nginx перечитает конфиг без перезагрузки
kill -TERM PID                    # Graceful shutdown
kill -KILL PID                    # Force kill (как kill -9)

# ПОВЕДЕНИЕ СЕРВИСОВ

# Хороший сервис:
# 1. На SIGTERM - gracefully shutdown (закрыть соединения, сохранить данные)
# 2. На SIGHUP - перечитать конфиг без downtime'а

# Плохой сервис:
# - Игнорирует SIGTERM (нужно force kill)
# - Не поддерживает SIGHUP (нужна перезагрузка)
```

---

## ЧАСТЬ 4: Управление пользователями и группами

### Просмотр пользователей и групп

```bash
# ПОЛЬЗОВАТЕЛИ

cat /etc/passwd                   # Список всех пользователей
# Формат: username:x:UID:GID:Real Name:Home Dir:Shell

# Текущий пользователь
whoami                            # Текущий username
id                                # UID, GID, groups
id username                       # Инфо о конкретном пользователе

# Кто залогинен
who                               # Кто залогинен сейчас
w                                 # Кто и что делает

# ГРУППЫ

cat /etc/group                    # Список всех групп
# Формат: groupname:x:GID:members

groups                            # Группы текущего пользователя
groups username                   # Группы конкретного пользователя
```

### Создание и удаление пользователей

```bash
# СОЗДАНИЕ ПОЛЬЗОВАТЕЛЯ

sudo useradd username             # Создать базового пользователя
sudo useradd -m -s /bin/bash username  # + home dir + shell
sudo useradd -m -G sudo username  # + в группу sudo

# УДАЛЕНИЕ ПОЛЬЗОВАТЕЛЯ

sudo userdel username             # Удалить пользователя (home остаётся)
sudo userdel -r username          # Удалить пользователя + home

# ИЗМЕНЕНИЕ ПОЛЬЗОВАТЕЛЯ

sudo usermod -s /bin/zsh username # Изменить shell
sudo usermod -aG sudo username    # Добавить в группу sudo (-a = append)
sudo usermod -d /new/home username  # Изменить home директорию

# УСТАНОВКА ПАРОЛЯ

sudo passwd username              # Установить пароль для пользователя
passwd                            # Изменить свой пароль

# БЛОКИРОВКА ПОЛЬЗОВАТЕЛЯ

sudo passwd -l username           # Заблокировать (lock)
sudo passwd -u username           # Разблокировать (unlock)
```

### Создание и управление группами

```bash
# СОЗДАНИЕ ГРУППЫ

sudo groupadd groupname           # Создать группу

# УДАЛЕНИЕ ГРУППЫ

sudo groupdel groupname           # Удалить группу

# ДОБАВЛЕНИЕ В ГРУППУ

sudo usermod -aG groupname username  # Добавить user в group

# УДАЛЕНИЕ ИЗ ГРУППЫ

sudo gpasswd -d username groupname   # Удалить user из group

# ПЕРЕМЕНА ГРУППЫ ФАЙЛА

sudo chgrp groupname file.txt     # Изменить группу файла
sudo chgrp -R groupname dirname   # Рекурсивно
```

### Sudo (супер-пользовательские привилегии)

```bash
# ЗАПУСК КОМАНДЫ С SUDO

sudo command                      # Запустить с root правами

# КОНФИГУРАЦИЯ SUDO

sudo visudo                       # Редактировать /etc/sudoers БЕЗОПАСНО!

# В /etc/sudoers добавить:

# Пользователю разрешить всё
username ALL=(ALL) ALL

# Пользователю разрешить без пароля
username ALL=(ALL) NOPASSWD: ALL

# Пользователю разрешить только конкретные команды
username ALL=(ALL) /usr/bin/apt, /usr/bin/systemctl

# Группе разрешить всё
%groupname ALL=(ALL) ALL

# ПРОСМОТР SUDO LOG

sudo journalctl SYSLOG_IDENTIFIER=sudo

# СОВЕТЫ

sudo -i                           # Войти как root (с окружением root)
sudo -s                           # Shell как root (с твоим окружением)
sudo -u username command          # Запустить как конкретный пользователь
```

---

## ЧАСТЬ 5: Окружение и переменные среды

### Переменные окружения

```bash
# ПОСМОТРЕТЬ ВСЕ

env                               # Все переменные окружения
printenv                          # Альтернатива env

# ПОСМОТРЕТЬ КОНКРЕТНУЮ

echo $PATH                        # Посмотреть PATH
echo $HOME                        # Домашняя директория
echo $USER                        # Текущий пользователь
echo $SHELL                       # Текущий shell

# ОСНОВНЫЕ ПЕРЕМЕННЫЕ

PATH          # Пути где искать команды
HOME          # Домашняя директория
USER          # Текущий пользователь
SHELL         # Текущий shell
PWD           # Текущая директория
LANG          # Язык системы
TERM          # Тип терминала
EDITOR        # Редактор по-умолчанию
```

### Установка переменных

```bash
# ВРЕМЕННО (для текущей сессии)

export VAR_NAME="value"           # Установить переменную
VAR_NAME="value"                  # Без export - только для скриптов

# ПОСТОЯННО

# Добавить в ~/.bashrc или ~/.zshrc:
export PATH=$PATH:/new/path
export EDITOR=vim

# Затем:
source ~/.bashrc                  # Перечитать конфиг

# ИСПОЛЬЗУЕМЫЕ ПЕРЕМЕННЫЕ

# В скриптах:
echo "The path is: $PATH"
ls $HOME/documents

# В командах:
cd $HOME
which $EDITOR
```

### PATH - поиск команд

```bash
# PATH - это список директорий где shell ищет команды

echo $PATH
# /usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin

# Когда вводишь: firefox
# Shell ищет:
# /usr/local/bin/firefox  -> не найдено
# /usr/bin/firefox        -> найдено! запускает

# ДОБАВИТЬ СВОЮ ПАПКУ В PATH

# Вариант 1: Временно
export PATH=$PATH:/home/user/scripts

# Вариант 2: Постоянно (в ~/.bashrc)
export PATH=$PATH:/home/user/scripts

# Вариант 3: Правильно (в /usr/local/bin)
cp myscript /usr/local/bin/
chmod +x /usr/local/bin/myscript
myscript  # Будет найдено везде
```

---

## ЧАСТЬ 6: Перенаправление ввода-вывода (I/O Redirection)

### Основные операторы

```bash
# СТАНДАРТНЫЕ ПОТОКИ

stdin  (0)  # Вход (клавиатура)
stdout (1)  # Выход (экран)
stderr (2)  # Ошибки (экран)

# ПЕРЕНАПРАВЛЕНИЕ ВЫВОДА

command > file.txt                # Перенаправить stdout в файл
command >> file.txt               # Добавить (append) в файл
command 2> errors.txt             # Перенаправить stderr
command > output.txt 2> errors.txt # Оба потока

# ОБЪЕДИНИТЬ stdout И stderr

command &> all.txt                # Оба потока в один файл
command 2>&1                      # Перенаправить stderr в stdout

# ПЕРЕНАПРАВЛЕНИЕ ВВОДА

command < input.txt               # Использовать файл как stdin

# ТРУБЫ (PIPES)

command1 | command2               # Вывод command1 -> вход command2
ls -la | grep ".txt"              # Список текстовых файлов
cat /var/log/syslog | grep error  # Ошибки в логах
```

### Примеры с редактором

```bash
# Создать файл
echo "Hello world" > file.txt

# Добавить строку
echo "Another line" >> file.txt

# Скопировать с перенаправлением
cat source.txt > copy.txt

# Сохранить ошибки в лог
command 2> error.log

# Сохранить вывод и ошибки
command > output.log 2>&1

# Отправить вывод в никуда
command > /dev/null 2>&1

# Здесь /dev/null - специальный файл который "поглощает" всё
```

---

## ЧАСТЬ 7: Быстрые советы и трюки

### Шпаргалка команд

```bash
# ИНФОРМАЦИЯ О СИСТЕМЕ

uname -a                          # Общая инфо о системе
hostnamectl                       # Имя хоста, ОС версия
lsb_release -a                    # Версия дистрибутива
uptime                            # Сколько система работает
whoami                            # Текущий пользователь

# ДИСК И ПАМЯТЬ

df -h                             # Место на диске
du -sh ~/*                        # Размер папок в home
free -h                           # Память

# ЖЕЛЕЗО

lscpu                             # Инфо о CPU
lsblk                             # Диски и партиции
lspci                             # PCI устройства
lsusb                             # USB устройства

# ПРОЦЕССЫ

ps aux | grep firefox             # Найти процесс
top                               # Real-time мониторинг
kill -9 PID                       # Убить процесс

# ФАЙЛЫ

find / -name "*.txt"              # Поиск файлов
grep "pattern" file               # Поиск в содержимом
ls -lah                           # Список с подробностями
```

### История команд

```bash
# ПРОСМОТР ИСТОРИИ

history                           # Вся история
history | tail -20                # Последние 20 команд
history | grep "search"           # Поиск в истории

# ИСПОЛЬЗОВАНИЕ ИСТОРИИ

!!                                # Последняя команда
!sudo                             # Последняя команда начинающаяся с sudo
!403                              # Команда номер 403 из истории

# СОХРАНЕНИЕ И ОТКЛЮЧЕНИЕ

Ctrl+R                            # Интерактивный поиск в истории
export HISTSIZE=5000              # Хранить больше команд
export HISTFILE=~/.bash_history   # Где хранится история

# ОЧИСТИТЬ ИСТОРИЮ

history -c                        # Очистить в памяти
history -w                        # Очистить в файле
rm ~/.bash_history                # Удалить файл истории
```

---

## ЧАСТЬ 8: Текстовые фильтры и обработка

### Основные фильтры

```bash
# SORT (сортировка)

sort file.txt                     # Сортировать
sort -r file.txt                  # Обратная сортировка
sort -n file.txt                  # По числовому значению
sort -u file.txt                  # Удалить дубликаты

# UNIQ (уникальные строки)

uniq file.txt                     # Показать уникальные
sort file.txt | uniq              # Сортировать + уникальные
uniq -c file.txt                  # Считать повторения

# WC (подсчёт)

wc -l file.txt                    # Количество строк
wc -w file.txt                    # Количество слов
wc -c file.txt                    # Количество символов

# CUT (вырезание столбцов)

cut -d ',' -f 1 file.csv          # Первый столбец CSV
cut -c 1-10 file.txt              # Символы 1-10 каждой строки

# AWK (обработка текста)

awk '{print $1}' file             # Первый столбец
awk -F ',' '{print $1}' file.csv  # Первый столбец CSV

# SED (редактирование потока)

sed 's/old/new/g' file            # Заменить все old на new
sed -i 's/old/new/g' file         # Изменить файл на месте
sed '5d' file                     # Удалить 5 строку

# TR (трансформация символов)

tr 'a-z' 'A-Z' file               # Маленькие буквы в большие
tr -d ' ' file                    # Удалить пробелы

# HEAD / TAIL

head -n 10 file                   # Первые 10 строк
tail -n 10 file                   # Последние 10 строк
tail -f file                      # Real-time (для логов)
```

### Примеры комбинаций

```bash
# Найти самые большие файлы
find / -type f -exec ls -lh {} \; 2>/dev/null | sort -k5 -h | tail -20

# Подсчитать количество процессов по пользователю
ps aux | awk '{print $1}' | sort | uniq -c | sort -rn

# Найти дублирующиеся файлы
find . -type f -exec md5sum {} \; | awk '{print $1}' | sort | uniq -d

# Самые частые команды в истории
history | awk '{print $2}' | sort | uniq -c | sort -rn | head -20

# Анализ логов - найти ошибки и посчитать
grep ERROR /var/log/syslog | cut -d: -f1 | sort | uniq -c
```

---

## ЧАСТЬ 9: Шпаргалка (быстрый справочник)

### Основные операции

```bash
# ФАЙЛЫ И ПАПКИ
ls, cd, pwd, mkdir, rmdir, rm, cp, mv, touch

# ПРОСМОТР
cat, head, tail, less, grep

# ПОИСК
find, grep, locate

# ПРАВА
chmod, chown, chgrp, ls -la

# ПОЛЬЗОВАТЕЛИ
useradd, userdel, usermod, sudo, passwd

# ПРОЦЕССЫ
ps, top, kill, nice, renice

# ПЕРЕНАПРАВЛЕНИЕ
>, >>, <, |, 2>, &>
```

### Популярные комбинации

```bash
# Посмотреть файл в реальном времени (для логов)
tail -f /var/log/syslog

# Найти что-то большое
du -sh /* | sort -h | tail

# Убить зависший процесс
ps aux | grep firefox | grep -v grep | awk '{print $2}' | xargs kill -9

# Сделать backup
cp -r original backup-$(date +%Y-%m-%d)

# Быстрый поиск файла
find / -name "filename" 2>/dev/null

# Подробный список с сортировкой по времени
ls -lat | head -20
```

---

## ЧАСТЬ 10: Лучшие практики

### Dos and Don'ts

```
✅ ДЕЛАЙТЕ:
- chmod 755 для исполняемых файлов
- chmod 644 для обычных файлов
- chmod 600 для приватных файлов/конфигов
- Регулярно проверяйте диск (df -h)
- Используйте sudo для привилегированных операций
- Логируйте важные операции

❌ НЕ ДЕЛАЙТЕ:
- chmod 777 никогда
- rm -rf / шутки плохие
- Запускайте сервисы как root если не нужно
- Игнорируйте предупреждения о полном диске
- Забывайте про backup'ы
- chmod 755 на приватные конфиги
```

### Безопасность файлов

```bash
# SSH ключи - САМОЕ ВАЖНОЕ!
chmod 700 ~/.ssh                  # Папка
chmod 600 ~/.ssh/id_rsa           # Приватный ключ
chmod 644 ~/.ssh/id_rsa.pub       # Публичный ключ
chmod 644 ~/.ssh/authorized_keys  # Ключи других

# Конфигурационные файлы
chmod 600 ~/.bash_history         # История (если в ней пароли)
chmod 600 ~/.bashrc               # Конфиг shell

# Системные конфиги
ls -la /etc/shadow                # rw------- (только root!)
ls -la /etc/sudoers               # r--r----- (читаемо для группы sudo)
```

---

## Связанные заметки

### → Логическое продолжение (основной путь)
- [[linux-file-permissions]] - подробно о правах доступа (chmod, chown)
- [[linux-package-managers]] - выбор дистрибутива и менеджера пакетов

### ↔ Параллельно (рядом)
- [[systemd-basics]] - управление сервисами
- [[linux-backup-strategy]] - резервное копирование (КРИТИЧНО!)

### 📚 Главный индекс
- [[00-start-here-index]] - полная навигация по базе

### Источники
- Linux man pages: man ls, man chmod, man ps
- GNU Coreutils documentation
- Linux Filesystem Hierarchy Standard (FHS)


---

Создано: 2026-01-03
Обновлено: 2026-01-03

