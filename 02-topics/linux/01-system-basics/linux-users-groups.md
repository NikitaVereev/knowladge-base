---
created: 2026-01-03
updated: 2026-01-03
tags: [linux, users, groups, system-administration, user-management, reference]
type: reference
---

# Управление пользователями и группами Linux

## Основная идея

Linux - **многопользовательская система**. Каждый файл и процесс принадлежит пользователю и группе. Управление пользователями и группами - это **основа безопасности и доступа**.

**Три основных компонента:**
- **Пользователи (Users)** - кто делает работу (root, user, www-data)
- **Группы (Groups)** - коллекции пользователей (docker, sudo, wheel)
- **UID/GID** - числовые идентификаторы (0=root, 1000+=обычные)

**Почему это критично:**
- Правильные пользователи = правильные права доступа
- Группы упрощают управление (вместо 100 юзеров = 1 группа)
- sudo зависит от группы wheel/sudo
- Docker, системные сервисы используют специальных пользователей

---

## ЧАСТЬ 1: Концепция UID, GID, пользователей и групп

### Идентификаторы (UID и GID)

```bash
# UID (User ID) - уникальный номер пользователя
# GID (Group ID) - уникальный номер группы

# Специальные значения:
0        = root (администратор)
1-99     = системные пользователи (nginx, mysql, www-data, и т.д.)
1000+    = обычные пользователи (первый обычный юзер = 1000)

# Пример:
root:x:0:0:root:/root:/bin/bash
user:x:1000:1000:User Name:/home/user:/bin/bash
www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin

#    ^  ^   ^  ^      ^         ^           ^
#    |  |   |  |      |         |           |
#    |  |   |  |      |         |           └─ shell (обычно /bin/bash)
#    |  |   |  |      |         └───────────── домашняя папка
#    |  |   |  |      └───────────────────── полное имя
#    |  |   |  └──────────────────────────── GID (основная группа)
#    |  |   └─────────────────────────────── UID
#    |  └───────────────────────────────── пароль (x = в /etc/shadow)
#    └─────────────────────────────────── имя пользователя
```

### Пользователи vs Группы

```
Пользователи (люди, сервисы):
- root       (администратор, UID 0)
- user       (обычный человек, UID 1000)
- www-data   (веб-сервер, UID 33)
- mysql      (база данных, UID 106)
- docker     (демон Docker, UID 999)

Группы (коллекции пользователей):
- root       (группа root, GID 0)
- wheel      (суперпользователи, GID 10)  [Arch/RHEL]
- sudo       (суперпользователи, GID 27)  [Debian/Ubuntu]
- docker     (пользователи Docker, GID 999)
- users      (обычные пользователи, GID 100)
- adm        (администраторы логов)
- dialout    (доступ к серийным портам)
```

### Иерархия доступа

```
root (UID 0)
├─ может читать/писать любой файл
├─ может запускать любую команду
└─ не подлежит ограничениям файловых прав

Группа wheel/sudo
├─ может запускать команды как root (через sudo)
├─ не может напрямую, только через sudo
└─ как использование sudo без пароля (в sudoers)

Обычный пользователь (UID 1000+)
├─ может читать свои файлы
├─ может писать в свои папки
├─ ограничен правами (644, 755, 600)
└─ не может трогать чужие файлы

Системные пользователи (UID 1-99)
├─ не для логина (shell = /usr/sbin/nologin)
├─ только для запуска сервисов
├─ примеры: www-data, mysql, postgres, docker
└─ не имеют домашней папки или она /usr/sbin
```

---

## ЧАСТЬ 2: Просмотр пользователей и групп

### Посмотреть текущего пользователя

```bash
# Кто я?
whoami
# user

# Подробная информация о текущем пользователе
id
# uid=1000(user) gid=1000(user) groups=1000(user),4(adm),24(cdrom),27(sudo),30(dip),46(plugdev),999(docker)
#         ^                ^                        ^
#         |                |                        └─ все группы (999=docker!)
#         |                └─ основная группа
#         └─ UID и имя

# Какой shell использую?
echo $SHELL
# /bin/bash

# Домашняя папка
echo $HOME
# /home/user
```

### Посмотреть конкретного пользователя

```bash
# Информация о пользователе (если существует)
id username
# uid=1000(user) gid=1000(user) groups=1000(user),999(docker)

# Смотреть в файле /etc/passwd
grep user /etc/passwd
# user:x:1000:1000:John Doe:/home/user:/bin/bash

# Смотреть системного пользователя
grep www-data /etc/passwd
# www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin
#                                              ^
#                                              └─ не может логиниться!

# Посмотреть информацию о root
id root
# uid=0(root) gid=0(root) groups=0(root)
```

### Посмотреть все пользователи (быстро)

```bash
# Список всех пользователей
cat /etc/passwd | cut -d: -f1
# root
# daemon
# bin
# sys
# sync
# ...
# user
# ...

# Только обычные пользователи (UID >= 1000)
awk -F: '$3 >= 1000 {print $1}' /etc/passwd
# user
# (только обычные)

# Только системные пользователи
awk -F: '$3 < 1000 && $3 > 0 {print $1}' /etc/passwd
# daemon
# bin
# www-data
# mysql
# ...
```

### Посмотреть группы

```bash
# Мои группы
groups
# user adm sudo docker

# Мои группы (с числами)
id -G
# 1000 4 27 999
#  ^    ^  ^  ^
#  |    |  |  └─ docker (999)
#  |    |  └──── sudo (27)
#  |    └─────── adm (4)
#  └──────────── основная группа (1000)

# Информация о группе
grep docker /etc/group
# docker:x:999:user1,user2

#    ^    ^   ^
#    |    |   └─ члены группы (разделены запятыми)
#    |    └───── пароль группы (x = нет)
#    └────────── название группы

# Все группы в системе
cat /etc/group

# Только те, к которым я принадлежу
groups
# или
id -Gn
# user adm sudo docker
```

### Посмотреть членов группы

```bash
# Члены группы docker
grep docker /etc/group
# docker:x:999:user,anotheruser

# Или использовать команду (если установлена)
members docker
# user
# anotheruser

# Члены группы sudo
getent group sudo
# sudo:x:27:user1,user2,user3
```

---

## ЧАСТЬ 3: Создание пользователя

### Команда useradd (низкоуровневая)

```bash
# Создать пользователя (базовая)
sudo useradd newuser

# Проверить что создан
id newuser
# uid=1001(newuser) gid=1001(newuser) groups=1001(newuser)

# Но НЕЛЬЗЯ залогиниться, нет пароля!
# sudo useradd newuser --не даёт пароль по умолчанию
```

### Команда adduser (высокоуровневая, удобнее)

```bash
# Создать пользователя (интерактивная, приветливая)
sudo adduser newuser

# Вывод:
# Adding user `newuser' ...
# Adding new group `newuser' (1001) ...
# Adding new user `newuser' (1001) with group `newuser' ...
# Creating home directory `/home/newuser' ...
# Copying files from `/etc/skel' ...
# New password: [вводишь пароль]
# Retype new password: [повторяешь]
# passwd: password updated successfully
# Changing the user information for newuser
# Enter the new value, or press enter for the default
# Full Name []: John Developer
# Room Number []: 
# Work Phone []: 
# Home Phone []: 
# Other []: 
# Is the information correct? [Y/n] y
```

### Практические примеры создания пользователя

```bash
# Создать обычного пользователя с домашней папкой
sudo useradd -m -s /bin/bash newuser
# -m = создать домашнюю папку (-m = --create-home)
# -s = shell (-s = --shell)

# Установить пароль
sudo passwd newuser
# New password: [вводишь]
# Retype new password: [повторяешь]

# Полная версия (всё сразу)
sudo useradd -m -s /bin/bash -d /home/newuser -c "John Developer" newuser

# Для системного пользователя (без логина)
sudo useradd -r -s /usr/sbin/nologin webservice
# -r = создать системного пользователя (UID < 1000)
# -s /usr/sbin/nologin = не может логиниться

# Проверить что создан
id newuser
getent passwd newuser
ls -la /home/ | grep newuser
```

---

## ЧАСТЬ 4: Создание группы

### Команда groupadd

```bash
# Создать группу
sudo groupadd developers

# Проверить что создана
grep developers /etc/group
# developers:x:1001:

# Посмотреть GID новой группы
getent group developers
# developers:x:1001:

# Создать группу с определённым GID
sudo groupadd -g 2000 special-group
# -g = задать GID

# Создать системную группу
sudo groupadd -r system-group
# -r = создать системную (GID < 1000)
```

### Практические примеры

```bash
# Группа для разработчиков
sudo groupadd developers

# Группа для проекта
sudo groupadd project-team

# Группа для веб-сервера
sudo groupadd -r webserver

# Проверить создание
getent group developers
# developers:x:1001:

getent group webserver
# webserver:x:999:
```

---

## ЧАСТЬ 5: Добавление пользователя в группу

### Команда usermod (изменить пользователя)

```bash
# Добавить пользователя в группу (ДОПОЛНИТЕЛЬНАЯ группа)
sudo usermod -aG docker user
# -a = добавить (add) а не заменить
# -G = группы (-G = --groups)
# user = имя пользователя

# Проверить
id user
# uid=1000(user) gid=1000(user) groups=1000(user),999(docker)
#                                                        ^
#                                                        └─ docker добавлена!

# Добавить в несколько групп
sudo usermod -aG docker,sudo,adm user

# Проверить
groups user
# user adm sudo docker

# Заменить основную группу (без -a)
sudo usermod -g newgroup user
# -g = основная группа (без a)

# Заменить все группы (очень опасно!)
sudo usermod -G docker,sudo user
# БЕЗ -a = заменяет все группы!
# ОСТОРОЖНО!
```

### Добавить себя в группу (self-add)

```bash
# Добавить текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Проверить
id
# uid=1000(user) gid=1000(user) groups=1000(user),999(docker)

# ⚠️ ВАЖНО: нужно перезагрузить сессию!
# Способ 1: перезагрузиться
sudo reboot

# Способ 2: новая сессия (без перезагрузки)
newgrp docker

# Способ 3: запустить новый shell
bash
# и затем: groups
```

### Практические примеры

```bash
# Добавить пользователя в sudo (даст права администратора)
sudo usermod -aG sudo newuser

# Проверить
id newuser
# groups=...,sudo,...

# Добавить в docker (даст доступ к Docker)
sudo usermod -aG docker user

# Добавить в wheel (Arch/RHEL, для sudo)
sudo usermod -aG wheel user

# Добавить в audio (для доступа к звуку)
sudo usermod -aG audio user

# Добавить в multiple groups (разработчик)
sudo usermod -aG docker,sudo,adm,users developer
```

---

## ЧАСТЬ 6: Удаление пользователя и группы

### Удалить пользователя

```bash
# Удалить пользователя (оставить домашнюю папку)
sudo userdel olduser
# Домашняя папка остаётся в /home/olduser/

# Удалить пользователя И домашнюю папку
sudo userdel -r olduser
# -r = удалить домашнюю папку и почту

# Проверить что удалён
id olduser
# id: 'olduser': no such user

# Проверить в файле
grep olduser /etc/passwd
# (ничего, удалён)
```

### Удалить группу

```bash
# Удалить группу
sudo groupdel oldgroup

# Проверить что удалена
grep oldgroup /etc/group
# (ничего, удалена)

# Если группа ещё используется (есть пользователи)
# Error: group 'developers' still in use by uid 1001
# Решение: сначала удалить/изменить пользователей
```

### Практические примеры

```bash
# Удалить временного пользователя
sudo userdel -r temp-user

# Удалить больше не нужную группу
sudo groupdel old-project

# Удалить пользователя, но оставить его файлы (для архива)
sudo userdel user-to-keep-files
# /home/user-to-keep-files/ остаётся
```

---

## ЧАСТЬ 7: Управление паролями

### Команда passwd

```bash
# Установить/изменить свой пароль
passwd
# Current password: [вводишь старый]
# New password: [вводишь новый]
# Retype new password: [повторяешь]
# passwd: password updated successfully

# Установить пароль другому пользователю (как root)
sudo passwd newuser
# New password: [вводишь]
# Retype new password: [повторяешь]

# Удалить пароль (не рекомендуется!)
sudo passwd -d user
# -d = удалить пароль

# Блокировать пользователя
sudo passwd -l user
# -l = lock (заблокировать)

# Разблокировать пользователя
sudo passwd -u user
# -u = unlock

# Установить пароль действуют на X дней (затем заставит изменить)
sudo passwd -e user
# -e = истечение

# Посмотреть информацию о пароле
sudo chage -l user
# Last password change                    : Jan 01, 2024
# Password expires                        : never
# Password inactive                       : never
```

### Практические примеры

```bash
# Создать пользователя и установить пароль
sudo useradd -m newuser
sudo passwd newuser

# Заблокировать компрометированный аккаунт
sudo passwd -l hacked-user

# Затем удалить его
sudo userdel -r hacked-user

# Установить пароль действует месяц
sudo chage -M 30 user
# M = max (максимум дней до истечения)
```

---

## ЧАСТЬ 8: Управление sudo (суперпользователь)

### Концепция sudo

```bash
# sudo = "superuser do" (выполнить как администратор)
# Синтаксис:
sudo команда

# Примеры:
sudo apt update         # обновить пакеты (нужны права)
sudo reboot             # перезагрузиться
sudo passwd user        # изменить пароль пользователя

# Первый раз запросит пароль, потом кэширует (~15 минут)
[sudo] password for user: [вводишь]
```

### Добавить пользователя в sudoers (дать права sudo)

```bash
# Способ 1: добавить в группу sudo (Debian/Ubuntu)
sudo usermod -aG sudo newuser

# Способ 2: добавить в группу wheel (Arch/RHEL)
sudo usermod -aG wheel newuser

# Проверить
sudo -v
# это проверит может ли текущий пользователь делать sudo

# Если не в группе:
sudo: user: command not found
```

### Редактировать sudoers (опасная операция!)

```bash
# ВСЕГДА используй visudo! (проверяет синтаксис)
sudo visudo

# Типичная конфигурация:
# %sudo ALL=(ALL:ALL) ALL
# %wheel ALL=(ALL:ALL) ALL

# Означает:
# %sudo = группа sudo
# ALL = на всех хостах
# (ALL:ALL) = может быть кто угодно, в какой угодно группе
# ALL = может выполнять любую команду

# Без пароля (очень опасно, но удобно для скриптов):
# %docker ALL=(ALL) NOPASSWD: ALL
# NOPASSWD = не требовать пароль

# ⚠️ ОСТОРОЖНО: ошибка в sudoers = потеря доступа!
# Всегда проверяй перед выходом!
```

### Практические примеры

```bash
# Добавить пользователя в sudo
sudo usermod -aG sudo newuser

# Проверить что может делать sudo
sudo -l
# User user may run the following commands on system:
#     (ALL : ALL) ALL

# Выполнить одну команду с sudo
sudo apt update

# Запустить sudo su (получить root shell)
sudo su

# Выполнить команду как другой пользователь (через sudo)
sudo -u otheruser whoami
# otheruser

# Посмотреть историю команд sudo
sudo journalctl SYSLOG_IDENTIFIER=sudo
```

---

## ЧАСТЬ 9: Практический сценарий - Создание разработчика

### Полный процесс создания нового разработчика

```bash
# ШАГ 1: Создать группу для проекта
sudo groupadd developers

# ШАГ 2: Создать пользователя
sudo useradd -m -s /bin/bash -c "John Developer" john

# ШАГ 3: Установить пароль
sudo passwd john

# ШАГ 4: Добавить в группы
sudo usermod -aG sudo,docker,developers john
# sudo = может использовать sudo (администратор)
# docker = может использовать Docker
# developers = проектная группа

# ШАГ 5: Проверить
id john
# uid=1001(john) gid=1001(john) groups=1001(john),4(adm),27(sudo),999(docker),1001(developers)

groups john
# john adm sudo docker developers

# ШАГ 6: Переключиться на пользователя (проверить)
sudo -u john bash
# Теперь ты как john

# Выход
exit
```

### Сценарий: добавить существующего пользователя в проект

```bash
# ШАГ 1: Создать папку проекта
mkdir /home/projects/my-project

# ШАГ 2: Создать группу для проекта
sudo groupadd project-team

# ШАГ 3: Установить права (владелец = root, группа = project-team)
sudo chown root:project-team /home/projects/my-project
sudo chmod 770 /home/projects/my-project
# 7 (rwx) = owner (root) может всё
# 7 (rwx) = group (project-team) может всё
# 0 (---) = others ничего не могут

# ШАГ 4: Добавить пользователей в группу
sudo usermod -aG project-team user1
sudo usermod -aG project-team user2
sudo usermod -aG project-team user3

# ШАГ 5: Проверить что работает
# Пользователь user1 может писать в папку проекта?
sudo -u user1 touch /home/projects/my-project/test.txt
ls -la /home/projects/my-project/
# -rw-r--r-- 1 user1 project-team 0 Jan 01 12:00 test.txt
# ✅ Работает!
```

---

## ЧАСТЬ 10: Шпаргалка (быстрая справка)

### Самые полезные команды

```bash
# Кто я?
whoami, id, groups

# Создание
sudo useradd -m -s /bin/bash user
sudo adduser user (интерактивная)
sudo groupadd group

# Добавление в группу
sudo usermod -aG group user

# Удаление
sudo userdel user
sudo userdel -r user (с папкой)
sudo groupdel group

# Пароли
sudo passwd user
sudo passwd -l user (блокировать)
sudo passwd -u user (разблокировать)

# Просмотр
id user
groups user
getent passwd user
getent group group

# Sudo
sudo whoami (выполнить как root)
sudo -l (посмотреть права)
sudo visudo (редактировать sudoers)
```

### Файлы конфигурации

```bash
/etc/passwd          - информация о пользователях
/etc/shadow          - хэши паролей (только root!)
/etc/group           - информация о группах
/etc/sudoers         - конфигурация sudo
/etc/skel/           - шаблон домашней папки
```

### Типичные ошибки

```bash
# ❌ Забыл -a при добавлении в группу
sudo usermod -G docker user
# Это заменит ВСЕ группы! Пользователь потеряет sudo!

# ✅ Правильно:
sudo usermod -aG docker user
# -a = добавить, не заменять

# ❌ Неправильный пароль для sudo
[sudo] password for user: [неправильно]
# sudo: 1 incorrect password attempt
# Спросит снова (но блокирует на время)

# ✅ Установить пароль перед первым sudo
sudo passwd $USER

# ❌ Отредактировал /etc/sudoers вручную (БЕЗ visudo)
# Синтаксическая ошибка = потеря доступа!

# ✅ Всегда используй:
sudo visudo
# Проверяет синтаксис перед сохранением
```

---

## Связанные заметки

### ← Перед этим (предусловие)
- [[linux-file-permissions]] - права доступа (зависит от прав, владение)

### → Логическое продолжение
- [[linux-system-hardening]] - защита (управление пользователями это security)

### ↔ Применение
- [[systemd-basics]] - системные пользователи для сервисов (www-data, mysql, и т.д.)

### 📚 Главный индекс
- [[00-start-here-index]] - полная навигация по базе знаний

---

## Источники

- `man useradd` - подробная документация
- `man groupadd` - документация groupadd
- `man usermod` - документация usermod
- `man passwd` - документация passwd
- `man sudoers` - синтаксис sudoers
- Linux man-pages: user_namespaces(7)
- Arch Wiki: Users and groups

---

Создано: 2026-01-03