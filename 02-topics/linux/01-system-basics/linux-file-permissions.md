---
created: 2026-01-03
updated: 2026-01-03
tags: [linux, permissions, file-management, system-administration, reference]
type: reference
---

# Права доступа Linux (File Permissions) - полный справочник

## Основная идея

Права доступа - **третий столп Linux** (после файловой системы и процессов). Они определяют кто может делать что с файлом/папкой.

**Три основных вопроса:**
- Кто может читать файл? (владелец, группа, другие)
- Кто может писать в файл? (изменять содержимое)
- Кто может исполнять файл? (запускать как программу)

**Почему это критично:**
- SSH ключи должны быть 600 (только владелец)
- Конфиги БД должны быть 640 (владелец + группа)
- Скрипты должны быть 755 (все могут читать/исполнять)
- Неправильные права = уязвимость или невозможность работать

---

## ЧАСТЬ 1: Символьная запись (rwx)

### Формат прав при `ls -l`

```bash
ls -l /etc/passwd
# -rw-r--r-- 1 root root 2345 Jan 01 12:00 /etc/passwd
#
# ^ ^  ^  ^
# | тип другие (others)
# |   группа (group)
# владелец (user)
```

### Расшифровка символов

```
Позиция    Значение                     Численно
─────────────────────────────────────────────────
1-й символ тип файла:
           - = обычный файл
           d = директория
           l = symlink (символическая ссылка)
           c = character device
           b = block device
           s = socket
           p = pipe (именованный канал)

2-4 символы права ВЛАДЕЛЬЦА (user):
           r = read (чтение)     = 4
           w = write (запись)    = 2
           x = execute (испълнение) = 1
           - = нет права          = 0

5-7 символы права ГРУППЫ (group):
           r, w, x, - = см. выше

8-10 символы права ДРУГИХ (others):
           r, w, x, - = см. выше
```

### Примеры интерпретации

```bash
-rw-r--r--  = обычный файл, 644
              владелец: rw- (4+2=6)
              группа:  r-- (4)
              другие:  r-- (4)

drwxr-xr-x  = директория, 755
              владелец: rwx (4+2+1=7)
              группа:  r-x (4+1=5)
              другие:  r-x (4+1=5)

-rwx------  = обычный файл, 700
              владелец: rwx (4+2+1=7)
              группа:  --- (0)
              другие:  --- (0)

-rw-------  = обычный файл, 600
              владелец: rw- (4+2=6)
              группа:  --- (0)
              другие:  --- (0)
```

---

## ЧАСТЬ 2: Численная запись (octal)

### Три цифры: собственник | группа | другие

```
chmod 755 file.txt
       ^^^
       |||
       ||└─ others = 5   (r-x = 4+1)
       |└── group = 5    (r-x = 4+1)
       └─── user = 7     (rwx = 4+2+1)
```

### Расчет каждой цифры

```
r (read)    = 4
w (write)   = 2
x (execute) = 1
- (none)    = 0

Примеры:
7 = 4+2+1 = rwx (полные права)
6 = 4+2   = rw- (читать + писать)
5 = 4+1   = r-x (читать + выполнять)
4 = 4     = r-- (только чтение)
3 = 2+1   = -wx (писать + выполнять, без чтения - редко)
2 = 2     = -w- (только запись)
1 = 1     = --x (только выполнение)
0 = 0     = --- (нет прав)
```

### Типичные права в табличке

| Число | Право | Используется для |
|-------|-------|------------------|
| 777 | rwxrwxrwx | Все могут всё (ОЧЕНЬ ОПАСНО!) |
| 755 | rwxr-xr-x | Исполняемые файлы, скрипты |
| 744 | rwxr--r-- | Обычные программы |
| 700 | rwx------ | Приватные файлы (только владелец) |
| 644 | rw-r--r-- | Обычные текстовые файлы, документы |
| 640 | rw-r----- | Конфиги с чувствительными данными |
| 600 | rw------- | SSH ключи, пароли, приватные данные |
| 400 | r-------- | Чтение-только (очень строго) |
| 000 | --------- | Никто не может (обычно по ошибке) |

---

## ЧАСТЬ 3: Команда chmod (изменить права)

### Синтаксис числовой

```bash
# Формат: chmod [число] файл
chmod 755 script.sh          # rwxr-xr-x
chmod 644 document.txt       # rw-r--r--
chmod 600 .ssh/id_rsa        # rw------- (SSH ключ)
chmod 400 secret.key         # r-------- (только чтение, суперзащита)

# На папку (рекурсивно)
chmod -R 755 /home/user/documents
chmod -R 700 ~/.ssh          # Папка SSH очень приватная

# Несколько файлов
chmod 644 file1.txt file2.txt file3.txt
```

### Синтаксис символьный (альтернатива)

```bash
# Формат: chmod [кто][+/-][право] файл
# Кто: u (user/владелец), g (group), o (others), a (all/все)
# Оператор: + (добавить право), - (убрать право), = (установить)

chmod u+x script.sh          # владельцу добавить execute
chmod g-w file.txt           # группе убрать write
chmod o-r document.txt       # другим убрать read
chmod a+x /path/to/script    # всем добавить execute

# Несколько операций
chmod u+rx,g+r,o-w file.txt  # все сразу

# Установить точно (= вместо +/-)
chmod u=rwx,g=rx,o=x file.txt  # владелец=rwx, группа=rx, другие=x
```

### Типичные сценарии

```bash
# СКРИПТ (должен быть исполняемым)
chmod 755 my-script.sh       # Все могут читать, владелец/группа исполнять
# или
chmod u+x my-script.sh       # Добавить execute владельцу

# ТЕКСТОВЫЙ ДОКУМЕНТ
chmod 644 document.txt       # Владелец может читать/писать, другие только читать
chmod 0600 sensitive-notes.txt  # Только владелец может читать

# КОНФИГУРАЦИОННЫЙ ФАЙЛ БД
chmod 640 /etc/mysql/my.cnf  # Владелец + группа (mysql) могут читать

# SSH КЛЮЧ (САМОЕ ВАЖНОЕ!)
chmod 600 ~/.ssh/id_rsa      # ТОЛЬКО владелец! Никому больше!
chmod 644 ~/.ssh/id_rsa.pub  # Публичный ключ - можно читать всем

# ПАПКА
chmod 755 /home/user/projects     # Стандартная папка (rwx для владельца)
chmod 700 ~/.ssh                  # SSH папка очень приватная
chmod 770 /shared/group-project   # Владелец + группа могут всё

# УДАЛИТЬ ВСЕ ПРАВА (очень редко)
chmod 000 file.txt           # Никто не может ничего (даже владелец!)
# Понадобится другой пользователь (или root) чтобы это исправить!
```

---

## ЧАСТЬ 4: Изменение владельца/группы (chown, chgrp)

### chown (change owner)

```bash
# Изменить владельца
sudo chown newuser file.txt           # Новый владелец
sudo chown newuser:newgroup file.txt  # Новый владелец и группа

# Примеры
sudo chown john document.txt          # Владелец = john
sudo chown john:developers script.sh  # Владелец = john, группа = developers

# Только группу
sudo chown :groupname file.txt        # Группа, владелец не меняется

# Рекурсивно (для папок)
sudo chown -R john:john /home/john/documents
sudo chown -R root:root /var/www/html
```

### chgrp (change group) - альтернатива

```bash
# Изменить только группу
sudo chgrp groupname file.txt
sudo chgrp -R developers /project/files
```

### Примеры для типичных ситуаций

```bash
# Сделать файл моим (если раньше был root'а)
sudo chown $USER my-config.conf

# Дать группе доступ к папке (например, для веб-сервера)
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html

# Сделать файл доступным для группы
sudo chown user:group shared-file.txt
sudo chmod 640 shared-file.txt        # rw- для user, r-- для group
```

---

## ЧАСТЬ 5: Специальные права (SetUID, SetGID, Sticky Bit)

### SetUID (Set User ID)

```bash
# Формат: 4xxx (четвёртая цифра)
chmod 4755 file              # SetUID включен
# Или символьно:
chmod u+s file

# Что это:
# Когда файл исполняется, он запускается с правами ВЛАДЕЛЬЦА,
# а не пользователя который его запустил

# Пример - sudo:
ls -l /usr/bin/sudo
# -rwsr-xr-x  (4755)
# Буква 's' вместо 'x' = SetUID!
# Значит sudo запускается с правами root'а

# Опасно! SetUID файлы - частые уязвимости
```

### SetGID (Set Group ID)

```bash
# Формат: 2xxx (четвёртая цифра)
chmod 2755 directory         # SetGID на папку
chmod g+s directory

# Что это на папке:
# Файлы созданные в папке наследуют группу папки,
# а не группу создателя файла

# Пример:
mkdir /shared
sudo chown root:developers /shared
sudo chmod 2770 /shared

# Теперь любой из группы developers может создавать файлы,
# и они автоматически будут принадлежать группе 'developers'
```

### Sticky Bit

```bash
# Формат: 1xxx (четвёртая цифра)
chmod 1777 directory         # Sticky bit на папку
chmod +t directory

# Что это:
# Файлы в папке может удалить ТОЛЬКО их владелец,
# даже если папка открыта для всех (777)

# Пример - /tmp:
ls -ld /tmp
# drwxrwxrwt (1777)
# Буква 't' вместо 'x' = Sticky bit!
# Значит в /tmp все могут создавать файлы,
# но удалять только свои, не чужие!

# Используется в shared/public папках
```

### Все вместе

```bash
# SetUID + SetGID + rwx для всех
chmod 6777 file              # 6 = 4+2 (SetUID + SetGID)

# SetUID + write + execute
chmod 4755 file              # Обычно для приложений

# Sticky bit на public папку
chmod 1777 /public/uploads   # Все могут загружать, но удалять только свои
```

---

## ЧАСТЬ 6: Просмотр текущих прав

```bash
# Посмотреть права одного файла
ls -l file.txt
# -rw-r--r-- 1 user group 1024 Jan 01 12:00 file.txt

# Посмотреть права папки и содержимого
ls -lR /home/user/

# Посмотреть права как числа (численно)
stat file.txt | grep Access
# Access: (0644/-rw-r--r--)

# Посмотреть кто владелец и группа
ls -l /etc/passwd
# -rw-r--r-- 1 root root ...
# владелец: root, группа: root

# Найти все файлы с определёнными правами
find . -perm 777           # Найти 777
find . -perm /u+s          # Найти с SetUID
find . -type d -perm 755   # Найти папки 755
```

---

## ЧАСТЬ 7: Практические правила безопасности

### ✅ ДЕЛАЙТЕ:

```bash
# SSH ключи - САМОЕ ВАЖНОЕ!
chmod 700 ~/.ssh                    # Папка SSH
chmod 600 ~/.ssh/id_rsa             # Приватный ключ (ТОЛЬКО владелец!)
chmod 644 ~/.ssh/id_rsa.pub         # Публичный ключ (читать всем ок)
chmod 644 ~/.ssh/authorized_keys    # Ключи других

# Конфигурационные файлы
chmod 644 ~/.bashrc                 # Конфиг shell
chmod 600 ~/.bash_history           # История (может содержать пароли)
chmod 600 ~/.config/app/config.ini  # Приватные конфиги

# Системные файлы
chmod 755 /usr/bin/myapp            # Исполняемые файлы для всех
chmod 644 /etc/app.conf             # Конфиги (root может писать)
chmod 640 /etc/mysql/my.cnf         # Конфиги БД (группа mysql читает)

# Скрипты
chmod 755 ~/bin/my-script.sh        # Скрипты общего назначения
chmod 700 ~/scripts/private.sh      # Приватные скрипты
```

### ❌ НЕ ДЕЛАЙТЕ:

```bash
# ОЧЕНЬ ПЛОХО!
chmod 777 anything                  # Никогда! Все могут всё!
chmod 666 file                      # Все могут писать - опасно!
chmod -R 777 /home/user/            # Весь дом открыт - кошмар!

# КРИТИЧНО ПЛОХО!
chmod 644 ~/.ssh/id_rsa             # SSH ключ читают все - скомпрометирован!
chmod 777 ~/.ssh                    # SSH папка открыта всем - уязвима!

# По ошибке плохо:
chmod 000 important-file            # Никто не может, даже владелец!
                                    # Потребуется root чтобы исправить
```

### Оптимальные права для типичных файлов

```bash
# Документы
chmod 644 report.pdf                # rw-r--r-- (владелец пишет, все читают)
chmod 600 personal-notes.txt        # rw------- (только я)

# Исходный код
chmod 644 src/main.cpp              # Обычно 644
chmod 755 configure                 # Скрипты конфигурации

# Веб
chmod 644 index.html                # Статические файлы
chmod 755 /var/www/html             # Веб папка
chmod 640 .htaccess                 # Конфиги веб-сервера (www-data читает)

# БД
chmod 640 database.conf             # Только владелец + группа db
chmod 600 private.key               # Ключи шифрования

# Папки (обычно)
chmod 755 ~/projects                # Стандартная папка (rwx для меня)
chmod 750 ~/private                 # Мне + группе (но не другим)
chmod 700 ~/.gnupg                  # Очень приватная
```

---

## ЧАСТЬ 8: Шпаргалка (быстрая справка)

### Численные права (самые частые)

```
777 = rwxrwxrwx  (все могут всё - ОПАСНО!)
755 = rwxr-xr-x  (стандарт для исполняемых)
750 = rwxr-x---  (владелец + группа)
744 = rwxr--r--  (владелец может всё)
700 = rwx------  (только владелец - приватное)
644 = rw-r--r--  (стандарт для файлов)
640 = rw-r-----  (конфиги с данными)
600 = rw-------  (приватные файлы - SSH ключи!)
400 = r--------  (только чтение - максимальная защита)
000 = ---------  (никто ничего - опасно!)
```

### Самые полезные команды

```bash
chmod 755 file          # Сделать исполняемым (стандарт)
chmod 644 file          # Сделать обычным текстовым файлом
chmod 600 file          # Сделать приватным (только я)
chmod 755 -R dir        # Рекурсивно для папки
chmod +x file           # Добавить execute
chmod -x file           # Убрать execute
sudo chown user:group file  # Изменить владельца
sudo chown -R user:group dir  # Рекурсивно
ls -l file              # Посмотреть текущие права
stat file               # Подробная информация
```

---

## ЧАСТЬ 9: Решение проблем

### "Permission denied" при запуске скрипта

```bash
./my-script.sh
# -bash: ./my-script.sh: Permission denied

# Решение:
chmod +x my-script.sh
# или
chmod 755 my-script.sh

./my-script.sh          # Теперь работает
```

### "Permission denied" при чтении файла

```bash
cat /root/secret.txt
# cat: /root/secret.txt: Permission denied

# Причина: файл принадлежит root, ты не можешь читать
ls -l /root/secret.txt
# -rw------- 1 root root 1024 Jan 01 12:00 /root/secret.txt

# Решение 1: использовать sudo
sudo cat /root/secret.txt

# Решение 2: попросить root изменить права
sudo chmod 644 /root/secret.txt   # Теперь все могут читать
```

### SSH говорит "UNPROTECTED PRIVATE KEY FILE"

```bash
ssh -i ~/.ssh/id_rsa user@server
# @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
# @ WARNING: UNPROTECTED PRIVATE KEY FILE! @
# @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@

# Причина: права на ключ неправильные (не 600)
ls -l ~/.ssh/id_rsa
# -rw-r--r--  (644 - ПЛОХО!)

# Решение: исправить на 600
chmod 600 ~/.ssh/id_rsa
chmod 700 ~/.ssh
# Теперь работает!
```

### "No such file or directory" но файл существует

```bash
./my-script.sh
# bash: ./my-script.sh: No such file or directory

# ls -l показывает что файл есть!

# Причина: файл написан на другой ОС (Windows)
# или неправильно скопирован

# Решение:
dos2unix my-script.sh      # Конвертировать если Windows
chmod +x my-script.sh      # Добавить execute
./my-script.sh             # Работает

# или просто:
bash my-script.sh          # Запустить через bash явно
```

### Случайно установил 000 (никто не может ничего)

```bash
chmod 000 important.txt
# Теперь даже владелец не может читать!

# Решение (как root):
sudo chmod 644 important.txt

# Или если ты root:
chmod 644 important.txt
```

---

## ЧАСТЬ 10: Группы пользователей (напоминание)

```bash
# Посмотреть свою группу
groups
# user adm dialout sudo docker

# Посмотреть группу файла
ls -l file.txt
# -rw-r--r-- 1 user docker 1024 ...
#           ^владелец ^группа

# Добавить себя в группу
sudo usermod -aG docker $USER
# Потребуется перезагрузка или:
newgrp docker

# Посмотреть членов группы
members docker
# или
grep docker /etc/group
# docker:x:999:user1,user2,user3
```

---

## Связанные заметки

### ← Перед этим (предусловие)
- [[linux-system-basics]] - основы Linux (файловая система, типы файлов)

### → После этого (логическое продолжение)
- [[linux-users-groups]] - управление пользователями и группами

### 🔐 Для SSH и безопасности
- SSH ключи ДОЛЖНЫ быть 600 (chmod 600 ~/.ssh/id_rsa)
- Это критично для безопасности!

### 📚 Главный индекс
- [[00-start-here-index]]

---

## Источники

- `man chmod` - полная документация
- `man chown` - документация chown
- `man ls` - опции для просмотра прав
- Arch Wiki: File permissions
- Linux man-pages: access(2), chmod(1)

---

Создано: 2026-01-03

