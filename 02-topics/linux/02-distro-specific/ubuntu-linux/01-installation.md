---
created: 2026-01-06
updated: 2026-01-06
type: reference
---

# Установка Ubuntu

## ⚠️ ТРЕБОВАНИЯ

- **USB флешка** — минимум 2GB (рекомендуется 4GB+)
- **Компьютер с UEFI** — BIOS режим устаревший (не рассматривается)
- **Интернет** — обязательно! (кабель или Wi-Fi)
- **Резервная копия** — обязательна при dual-boot

---

## 🔧 ПОДГОТОВКА: Создание загрузочной USB

### На Windows (Rufus или Balena Etcher)

**Способ 1: Rufus**
1. Скачайте Ubuntu ISO → https://ubuntu.com/download/desktop (24.04 LTS)
2. Скачайте Rufus → https://rufus.ie/
3. Запустите Rufus:
   - **Device**: выберите USB флешку
   - **Boot selection**: выберите ISO файл
   - **Partition scheme**: `GPT` (для UEFI)
   - **File system**: `FAT32`
   - **Нажмите**: START
4. Дождитесь завершения

**Способ 2: Balena Etcher (проще)**
1. Скачайте → https://www.balena.io/etcher/
2. Запустите, выберите ISO и USB
3. Нажмите Flash

### На Linux (dd или gnome-disks)

**Способ 1: dd**
```bash
# Узнайте имя USB
lsblk

# Размонтируйте если примонтирована
sudo umount /dev/sdX*

# Запишите ISO (⚠️ убедитесь в правильности /dev/sdX!)
sudo dd if=~/Downloads/ubuntu-24.04-desktop.iso of=/dev/sdX bs=4M status=progress && sync
```

**Способ 2: GNOME Disks (графический)**
```bash
gnome-disks
# Выберите USB → Format → Restore Disk Image → выберите ISO
```

---

## 🚀 ЗАГРУЗКА С USB

При включении компьютера нажимайте:
- **ThinkPad** → **F12** (или Ctrl+Alt+Del)
- **Dell** → **F12** (или Esc)
- **HP** → **Esc** (или F9)
- **ASUS/Gigabyte** → **Del** (или F2)
- **MSI** → **Delete** (или F11)

Выберите USB в меню загрузки.

---

## 📋 УСТАНОВКА: Subiquity

Subiquity — современный графический установщик Ubuntu.

### 1. Язык

Выберите **English** или **Русский**.

### 2. Раскладка клавиатуры

Выберите **ru** (русская) или **us** (английская).

### 3. Подключение к интернету

**Wi-Fi:**
- Выберите сеть
- Введите пароль

**Ethernet:**
- Обычно подключается автоматически

**Пропустить:** Можно позже подключиться

### 4. Обновления и сторонние драйверы

```
Install updates while installing: ON (рекомендуется)
Install third-party software: ON (если нужны proprietary драйверы)
```

### 5. Тип установки (Storage)

#### Вариант A: ТОЛЬКО Ubuntu

```
Use an entire disk: ON
Select disk: /dev/sda (выбрать свой)
Use LVM with the new Ubuntu installation: ON
Encrypt your home directory: OFF (для простоты)
```

Система создаст:
- `/dev/sda1` → 550MB EFI System
- `/dev/sda2` → остаток Linux filesystem

#### Вариант B: Dual-boot (Windows + Linux)

```
Custom storage layout
```

Вручную создайте:
```
/dev/sda1 ~ 550M    EFI System (существует или создать новый)
/dev/sda2 ~ 200GB   Windows (СУЩЕСТВУЕТ, НЕ ТРОГАТЬ!)
/dev/sda3 ~ остаток Linux filesystem (создать новый)
```

**⚠️ КРИТИЧНО:** Никогда не форматируйте Windows раздел!

### 6. Профиль (Profile)

```
Your name: John Doe
Your server's name (hostname): john-pc
Pick a username: john
Choose a password: ••••••••
Require a password to log in: ON
```

### 7. SSH Server (опционально)

```
Install OpenSSH server: OFF (на Desktop не нужен)
```

Включайте только если настраиваете сервер.

### 8. Featured Server Snaps

Пропустите (для сервера, не desktop).

### 9. Проверка и установка

Проверьте все параметры, затем:
```
Continue → Install
```

Установка займет **5-15 минут**.

### 10. Перезагрузка

```
Installation complete
Reboot Now
```

**ИЗВЛЕКИТЕ ФЛЕШКУ** перед перезагрузкой!

---

## ✅ ПОСЛЕ ПЕРВОЙ ЗАГРУЗКИ

### Логиниться

```
Username: john
Password: ••••••
```

### Обновить систему

```bash
sudo apt update
sudo apt upgrade -y
sudo apt dist-upgrade -y
```

**⚠️ ВСЕГДА** выполняйте после установки!

### Установить необходимое

```bash
# Если забыли при установке
sudo apt install firefox
sudo apt install vlc
sudo apt install git
sudo apt install curl
sudo apt install vim

# Если забыли видеодрайверы
sudo apt install nvidia-driver-550        # NVIDIA
sudo apt install amdgpu xwayland          # AMD
```

---

## 🚨 ПРОБЛЕМЫ И РЕШЕНИЯ

### Ошибка: "Failed to write to boot medium"

```bash
# USB может быть защищена от записи или повреждена
# Попробуйте другую флешку

# Или отформатируйте текущую
sudo umount /dev/sdX*
sudo mkfs.fat -F 32 /dev/sdX
# и запишите ISO заново
```

### Экран чёрный после загрузки

```bash
# Нажмите Ctrl+Alt+T для терминала
# или Ctrl+Alt+F2 (TTY2)

# Установите видеодрайверы
sudo apt install nvidia-driver-550   # NVIDIA
sudo apt install amdgpu              # AMD
sudo reboot
```

### Интернет не работает

```bash
# Перезагрузить сеть
sudo systemctl restart NetworkManager

# или
sudo systemctl restart systemd-networkd

# Проверить статус
ip link
ip addr
```

### Dual-boot: Windows не в меню GRUB

```bash
# Установить os-prober
sudo apt install os-prober

# Пересоздать GRUB конфиг
sudo grub-mkconfig -o /boot/grub/grub.cfg

# Перезагрузиться
sudo reboot
```

### Забыли пароль

```bash
# Загрузитесь с Live USB
sudo mount /dev/sda2 /mnt       # Linux раздел

# Если LVM (есть вопрос при установке)
sudo vgchange -ay               # активировать LVM
sudo mount /dev/ubuntu-vg/root /mnt

# Установите новый пароль
sudo chroot /mnt
passwd username

exit
reboot
```

---

## 📋 ШПАРГАЛКА УСТАНОВКИ

```bash
# USB флешка на Linux
sudo dd if=ubuntu-24.04-desktop.iso of=/dev/sdX bs=4M status=progress

# Установка (Subiquity)
# 1. Язык: English или Русский
# 2. Клавиатура: ru или us
# 3. Интернет: выбрать WiFi/Ethernet
# 4. Обновления: ON
# 5. Third-party: ON (если нужны драйверы)
# 6. Storage: целый диск или dual-boot
# 7. Профиль: имя пользователя и пароль
# 8. SSH: OFF (не нужен на desktop)
# 9. Install → Reboot

# После загрузки
sudo apt update && sudo apt upgrade -y
sudo apt install vim git curl firefox
```

---

## 🔗 ДАЛЬШЕ

[apt: управление пакетами](02-apt-guide.md)
