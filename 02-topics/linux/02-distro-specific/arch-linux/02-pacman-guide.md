---
created: 2026-01-06
updated: 2026-01-06
type: reference
---

# pacman: управление пакетами Arch

---

## 📚 ОСНОВНЫЕ КОМАНДЫ

### Обновление системы (rolling release)

```bash
sudo pacman -Syu             # обновить систему полностью
sudo pacman -Syy             # пересинхронизировать все репозитории (для recovery)
```

**Правило:** Обновляйтесь **минимум раз в неделю**.

### Установка пакетов

```bash
sudo pacman -S package       # один пакет
sudo pacman -S pkg1 pkg2 pkg3  # несколько пакетов
sudo pacman -S --needed pkg  # только если не установлен
sudo pacman -S --asdeps pkg  # установить как зависимость
```

### Удаление пакетов

```bash
sudo pacman -R package       # удалить (оставляет зависимые)
sudo pacman -Rs package      # удалить + неиспользуемые зависимости (РЕКОМЕНДУЕТСЯ)
sudo pacman -Rns package     # полное удаление + конфиги (только если уверены)
sudo pacman -Rdd package     # удалить БЕЗ проверки зависимостей (ОПАСНО!)
```

### Поиск пакетов

```bash
pacman -Ss term              # поиск в репозиториях (синопсис, описание)
pacman -Qs term              # поиск в установленных пакетах
pacman -S --search term      # альтернатива
```

### Информация о пакетах

```bash
pacman -Si package           # информация о пакете в репозитории
pacman -Qi package           # информация об установленном пакете
pacman -Ql package           # список файлов в пакете
pacman -Qo /path/to/file     # какой пакет содержит этот файл
```

### Просмотр установленных

```bash
pacman -Q                    # все установленные пакеты
pacman -Qu                   # пакеты с доступными обновлениями
pacman -Qe                   # явно установленные (не зависимости)
pacman -Qd                   # установленные как зависимости
pacman -Qdt                  # orphan пакеты (больше не нужны)
pacman -Qg base              # пакеты в группе 'base'
```

### Очистка системы

```bash
sudo pacman -Sc              # удалить старые версии пакетов из кэша (БЕЗОПАСНО)
sudo pacman -Scc             # очистить весь кэш (⚠️ потом нельзя откатить!)
sudo pacman -Rns $(pacman -Qdtq)  # удалить orphan пакеты (РЕКОМЕНДУЕТСЯ)
```

---

## ⚙️ КОНФИГУРАЦИЯ /etc/pacman.conf

### Стандартные опции (2026)

```ini
[options]
HoldPkg = pacman glibc linux  # не удалять даже при -Rdd
Architecture = auto
Color                         # цветной вывод
ParallelDownloads = 5         # одновременные загрузки
CheckSpace                    # проверять место на диске перед установкой
SigLevel = Required DatabaseOptional LocalOptional
LocalFileSigLevel = Optional  # для локальных .pkg.tar.zst
```

### Оптимизация (рекомендуется)

```ini
# Увеличить параллельные загрузки
ParallelDownloads = 10        # вместо 5 (если хороший интернет)

# Включить multilib (для 32-бит приложений, NVIDIA и т.д.)
[multilib]
Include = /etc/pacman.d/mirrorlist
```

### Как редактировать

```bash
sudo nano /etc/pacman.conf
# или
sudo vim /etc/pacman.conf

# После редактирования обновите систему
sudo pacman -Syu
```

---

## 🌍 ЗЕРКАЛА (mirrorlist)

### Переранжировать зеркала

```bash
# Найти 5 лучших зеркал по скорости
sudo pacman-key --init        # инициализировать ключи (первый раз)

# Обновить список зеркал из интернета
sudo reflector --country Russia --age 12 --sort score --save /etc/pacman.d/mirrorlist
```

### Установить reflector

```bash
sudo pacman -S reflector
```

---

## 🚨 ПРОБЛЕМЫ И РЕШЕНИЯ

### Ошибка: "error: target not found"

```bash
# Пакета нет или его переименовали
pacman -Ss exact-name         # поискать похожие

# Может быть перемещен в другой репозиторий
sudo pacman -Syu              # обновить список
```

### Ошибка: "error: failed to commit transaction (conflicting files)"

```bash
# Два пакета хотят один файл
sudo pacman -S --overwrite='*' package

# Или для одного файла
sudo pacman -S --overwrite /path/to/file package
```

### Ошибка: "error: could not satisfy dependencies"

```bash
# 1. Обновите систему полностью
sudo pacman -Syu

# 2. Удалите orphan пакеты
sudo pacman -Rns $(pacman -Qdtq)

# 3. Попробуйте снова
sudo pacman -S package
```

### Зависимость не найдена

```bash
# Посмотрите какая
sudo pacman -S package 2>&1 | grep "error:"

# Установите зависимость вручную
sudo pacman -S dependency

# Затем установите пакет
sudo pacman -S package
```

### Пакет заблокировал другой процесс

```bash
# Ошибка: "error: could not open lock file"
ps aux | grep pacman          # ищем зависший процесс

sudo killall -9 pacman        # если зависла
sudo rm /var/lib/pacman/db.lck  # удалить lock файл

sudo pacman -Syu              # попробовать снова
```

### Откатить пакет на старую версию

**Способ 1: из кэша (если есть)**
```bash
ls /var/cache/pacman/pkg/ | grep package
sudo pacman -U /var/cache/pacman/pkg/package-oldversion.tar.zst
```

**Способ 2: Arch Linux Archive**
```bash
# Установить downgrade
sudo pacman -S downgrade

# Использовать
sudo downgrade package
# Выбрать версию из списка
```

### После обновления: "error: could not determine cachedir"

```bash
# Кэш папка имеет проблемы
sudo rm -rf /var/cache/pacman/pkg/*
sudo pacman -Syu
```

---

## 📋 ШПАРГАЛКА pacman

```bash
# ⚠️ ЗОЛОТОЕ ПРАВИЛО
sudo pacman -Syu              # ВСЕГДА вместе!

# Установка
sudo pacman -S package        # один
sudo pacman -S pkg1 pkg2 pkg3 # несколько

# Удаление (РЕКОМЕНДУЕТСЯ -Rs)
sudo pacman -Rs package       # + неиспользуемые зависимости
sudo pacman -Rns package      # полное + конфиги

# Поиск
pacman -Ss term               # в репозиториях
pacman -Qs term               # в установленных

# Информация
pacman -Si package            # в репозитории
pacman -Qi package            # установленный
pacman -Ql package            # файлы пакета

# Очистка
sudo pacman -Sc               # старые версии (безопасно)
sudo pacman -Rns $(pacman -Qdtq)  # orphan пакеты

# Конфигурация
sudo nano /etc/pacman.conf    # редактирование
ParallelDownloads = 10        # ускорение
```

---

## 🔗 ВАЖНЫЕ ССЫЛКИ

- **Arch Wiki**: https://wiki.archlinux.org/title/Pacman
- **Официальная документация**: https://man.archlinux.org/man/pacman.8

---

## 🔗 ДАЛЬШЕ

[AUR (Arch User Repository)](./03-aur-guide.md)
