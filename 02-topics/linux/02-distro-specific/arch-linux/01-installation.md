---
created: 2026-01-06
updated: 2026-01-06
type: reference
---

# Установка Arch Linux

## ⚠️ ТРЕБОВАНИЯ

- **USB флешка** — минимум 2GB (рекомендуется 4GB+)
- **Компьютер с UEFI** — BIOS режим НЕ рассматривается (устаревший)
- **Интернет** — обязательно! (кабель или Wi-Fi)
- **Резервная копия** — обязательна при dual-boot

---

## 🔧 ПОДГОТОВКА: Создание загрузочной USB

### На Windows (Rufus)

1. Скачайте Arch ISO → https://archlinux.org/download/ (выберите **сиденье** или **magnet link**)
2. Скачайте **Rufus** → https://rufus.ie/
3. Запустите Rufus:
   - **Device**: выберите вашу USB флешку
   - **Boot selection**: нажмите кнопку и выберите `archlinux-*.iso`
   - **Partition scheme**: `GPT` (для UEFI)
   - **File system**: `FAT32`
   - **Нажмите**: START
4. Дождитесь завершения

### На Linux (dd)

```bash
# 1. Узнайте имя USB
lsblk

# 2. Размонтируйте если примонтирована
sudo umount /dev/sdX*          # X = буква вашей флешки (a, b, c...)

# 3. Запишите ISO
sudo dd if=~/Downloads/archlinux-*.iso of=/dev/sdX bs=4M status=progress && sync

# ⚠️ ВНИМАНИЕ: убедитесь что выбрали правильный диск! dd не просит подтверждения!
```

---

## 🚀 ЗАГРУЗКА С USB

При включении компьютера нажимайте:
- **ThinkPad** → **F12** (или Ctrl+Alt+Del)
- **Dell** → **F12** (или Esc)
- **HP** → **Esc** (или F9)
- **ASUS/Gigabyte** → **Del** (или F2)
- **MSI** → **Delete** (или F11)

Выберите USB в меню загрузки.

---

## 📋 УСТАНОВКА: archinstall TUI

После загрузки вы видите приглашение `root@archiso`.

### Запуск установщика

```bash
archinstall
```

Откроется **текстовый интерфейс** (TUI) с меню.

---

## 🎯 ШАГИ УСТАНОВКИ (по порядку)

### 1️⃣ Keyboard layout

```
> us              # английская
  ru              # русская
  de              # немецкая
  ...
```

Выберите **ru** (если русская раскладка) или **us**.

### 2️⃣ Mirror region

```
Rank mirrors: Yes/No → Yes
```

Выберите **Yes** — система найдет лучшие зеркала автоматически.

### 3️⃣ Disk configuration

**САМОЕ ВАЖНОЕ!**

```
Select disk layout:
> Use a best-effort default...  (рекомендуется)
  Manual partitioning
  Pre-mounted configuration
```

#### Вариант A: ТОЛЬКО Arch Linux

Выберите **"Use a best-effort default..."**

```
Select one of the disks to partition:
> /dev/sda
  /dev/sdb
  ...
```

Выберите свой диск (обычно **`/dev/sda`**). Система создаст:
- `/dev/sda1` → 550M EFI System
- `/dev/sda2` → остаток Linux filesystem

#### Вариант B: Dual-boot (Windows + Arch)

Выберите **"Manual partitioning"**, затем:

```
Select each partition to configure:
/dev/sda1 ~ 550M    EFI System  (новый, СОЗДАТЬ)
/dev/sda2 ~ 200GB   Windows     (существует, НЕ ТРОГАТЬ)
/dev/sda3 ~ остаток Linux FS   (новый, СОЗДАТЬ)
```

**⚠️ НИКОГДА не трогайте Windows раздел!**

### 4️⃣ Filesystem

```
Filesystem: 
> ext4             (стандартный и надежный)
  btrfs            (продвинутый, с snapshots)
  xfs              (для больших файлов)
```

Выберите **ext4** (если не уверены).

### 5️⃣ Encryption

```
Encrypt disk: Yes/No → No
```

Выберите **No** (для простоты).

### 6️⃣ Bootloader

```
Bootloader:
> GRUB            (рекомендуется)
  Systemd-boot   (для UEFI-only систем)
```

Выберите **GRUB**.

### 7️⃣ Network configuration

```
Select network configuration:
> Use NetworkManager   (графический интерфейс)
  Use iwd            (минимальный, для CLI)
  Copy ISO config    (текущая конфигурация)
```

Выберите **"Use NetworkManager"** (стандартный выбор).

### 8️⃣ Audio server

```
Audio server:
> pipewire         (новый, рекомендуется)
  pulseaudio       (старый, но стабильный)
  none             (без звука)
```

Выберите **pipewire** (современный выбор).

### 9️⃣ Graphics driver

```
Graphics driver:
> Intel            (Intel видеокарты)
  AMD              (AMD видеокарты)
  NVIDIA           (NVIDIA видеокарты)
  None             (использовать стандартный)
```

Выберите **свой**. Если не знаете:
```bash
lspci | grep -i vga   # покажет вашу видеокарту
```

### 🔟 Desktop environment

```
Desktop environment:
> GNOME            (красивое, полнофункциональное)
  KDE Plasma      (мощное, много опций)
  XFCE            (легкое, классическое)
  i3              (минимальное, тайловое)
  None            (только CLI)
```

**Рекомендации:**
- Новичку → **GNOME**
- Мощный компьютер → **KDE**
- Слабый компьютер → **XFCE**
- Опытному → **None** (настраивайте сами)

### 1️⃣1️⃣ Additional packages

```
Additional packages (Enter для пропуска):
> 
```

Добавляйте по одному, нажимая Enter:
```
vim                # текстовый редактор
git                # контроль версий
curl               # загрузка файлов
wget               # загрузка по FTP
htop               # мониторинг процессов
base-devel         # компиляция (для AUR)
firefox            # браузер
```

Просто введите строку и нажмите **Enter** для каждого.

### 1️⃣2️⃣ Profile

```
Select profile:
> default          (рекомендуется)
  xorg             (только X11)
```

Выберите **default**.

### 1️⃣3️⃣ Locale language

```
Locale language:
> en_US            (английский)
  ru_RU            (русский)
  ...
```

Выберите **ru_RU** (для русского) или **en_US**.

### 1️⃣4️⃣ Locale encoding

```
Locale encoding:
> UTF-8            (рекомендуется)
  ISO-8859-1       (старый)
```

Выберите **UTF-8**.

### 1️⃣5️⃣ Timezone

```
Timezone:
> Europe/Moscow    (если в России)
  UTC              (если хотите UTC)
  ...
```

Выберите **ваш часовой пояс**.

### 1️⃣6️⃣ Hostname

```
Hostname (имя компьютера):
> myarch
```

Введите **любое имя** (без пробелов, например `myarch`, `laptop`, `workstation`).

### 1️⃣7️⃣ Create user

```
Create a user: Yes/No → Yes
```

Выберите **Yes**.

```
Username: john
Password: ••••••
Confirm password: ••••••
Should this user be sudo? Yes/No → Yes
```

Введите имя пользователя (например `john`) и пароль. Выберите **Yes** для sudo.

### 1️⃣8️⃣ Root password

```
Set root password:
Password: ••••••
Confirm password: ••••••
```

Введите пароль для **root** пользователя. **ВАЖНО**: запомните его!

### 1️⃣9️⃣ Ready to install

```
Are you ready to install?
> Yes
  No
```

**Проверьте всё еще раз**, затем выберите **Yes**.

---

## ⏳ УСТАНОВКА (ожидание)

Система будет устанавливаться **5-15 минут**. Не прерывайте!

```
Installing packages...
Setting up GRUB...
Finalizing installation...
Installation complete!
```

После завершения:
```
Reboot now? Yes/No → Yes
```

**ИЗВЛЕКИТЕ ФЛЕШКУ** перед перезагрузкой!

---

## ✅ ПОСЛЕ ПЕРВОЙ ЗАГРУЗКИ

### Логиниться

```
login: john
password: ••••••
```

Введите имя пользователя и пароль.

### Обновить систему

```bash
sudo pacman -Syu
```

Всегда выполняйте это **сразу после установки**!

### Установить необходимое

```bash
# Если забыли что-то при установке
sudo pacman -S firefox
sudo pacman -S discord
sudo pacman -S vlc

# Для AUR помощника
sudo pacman -S base-devel git
git clone https://aur.archlinux.org/yay.git
cd yay && makepkg -si
```

---

## 🚨 ПРОБЛЕМЫ И РЕШЕНИЯ

### Ошибка: "Partition already in use"

```bash
# Диск используется другой программой
sudo umount /dev/sdX*
sudo fdisk -l /dev/sdX    # проверить состояние
# Начните заново
```

### После загрузки: Черный экран

```bash
# Нажмите Ctrl+Alt+T для терминала
# или Ctrl+Alt+F2 (TTY2)

# Переинсталлируйте видеодрайвер
sudo pacman -S nvidia nvidia-utils   # NVIDIA
sudo pacman -S amdgpu                # AMD
sudo reboot
```

### Интернет не работает после установки

```bash
# Перезагрузить сеть
sudo systemctl restart networkmanager
# или
sudo systemctl restart iwd

# Проверить подключение
ping 8.8.8.8

# Если не помогает
sudo systemctl enable --now networkmanager
```

### Забыли пароль пользователя

```bash
# Загрузитесь в режим single-user (в GRUB)
# Или загрузитесь с Live USB и смонтируйте систему
sudo mount /dev/sda2 /mnt
sudo chroot /mnt
passwd username    # новый пароль
exit
reboot
```

---

## ✨ ОБНОВЛЕНИЕ ARCHINSTALL

Если установочная флешка старая:

```bash
# На Live USB
sudo pacman -Sy archinstall

# Затем запустите как обычно
archinstall
```

---

## 📝 ШПАРГАЛКА УСТАНОВКИ

```bash
# USB флешка
sudo dd if=archlinux.iso of=/dev/sdX bs=4M status=progress

# Установка
archinstall

# Выборы в меню
Keyboard: ru или us
Mirrors: Yes
Disk: /dev/sda (выбрать свой)
Layout: Use best-effort default
Filesystem: ext4
Encryption: No
Bootloader: GRUB
Network: Use NetworkManager
Audio: pipewire
GPU: Intel/AMD/NVIDIA (выбрать свой)
Desktop: GNOME/KDE/XFCE/None
Packages: vim git curl wget htop base-devel
Locale: ru_RU.UTF-8 или en_US.UTF-8
Timezone: Europe/Moscow (выбрать свой)
Hostname: myarch (придумать имя)
Username: john
Root password: (придумать)

Ready to install? Yes
# ... ожидание
Reboot? Yes
# ИЗВЛЕКИТЕ ФЛЕШКУ!

# После загрузки
sudo pacman -Syu
```

---

## 🔗 ДАЛЬШЕ

[pacman: управление пакетами](./02-pacman-guide.md)
