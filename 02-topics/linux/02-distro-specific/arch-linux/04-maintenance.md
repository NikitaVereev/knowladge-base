---
created: 2026-01-06
updated: 2026-01-06
type: reference
---

# Обслуживание Arch Linux

## 🔄 ОБНОВЛЕНИЕ СИСТЕМЫ

### Золотое правило: обновляйтесь регулярно

```bash
sudo pacman -Syu             # минимум раз в неделю
```

**Почему регулярно?** Arch — rolling release. Пакеты обновляются постоянно. Если долго не обновляться, конфликты растут как снежный ком.

### После большого обновления перезагрузитесь

```bash
sudo reboot
```

Особенно после обновления:
- `linux` (kernel)
- `systemd` (система инициализации)
- `glibc` (стандартная библиотека C)

---

## 🧹 ОЧИСТКА СИСТЕМЫ (БЕЗОПАСНО)

### Удалить orphan пакеты (неиспользуемые)

```bash
# Посмотреть orphan пакеты
pacman -Qdtq

# Удалить
sudo pacman -Rns $(pacman -Qdtq)
```

**Orphan пакеты** — установлены, но ничего на них не ссылается. Безопасно удалить.

### Очистить кэш пакетов

```bash
# Удалить старые версии (БЕЗОПАСНО - можно откатить)
sudo pacman -Sc

# Удалить ВСЕ кэш (⚠️ ОПАСНО - потом нельзя откатить!)
sudo pacman -Scc
```

**Рекомендация:** Используйте только `-Sc` (удаление старых версий).

### Очистить логи systemd

```bash
# Посмотреть размер логов
journalctl --disk-usage

# Удалить логи старше 3 дней (БЕЗОПАСНО)
sudo journalctl --vacuum-time=3d

# Удалить логи старше месяца
sudo journalctl --vacuum-time=30d

# НИКОГДА не используйте без явного времени!
```

### Ограничить размер логов в будущем

```bash
sudo nano /etc/systemd/journald.conf

# Добавить или раскомментировать:
SystemMaxUse=100M
MaxRetentionSec=4weeks

# Затем перезагрузить systemd
sudo systemctl restart systemd-journald
```

### Очистить временные файлы

```bash
# /tmp очищается автоматически при перезагрузке
# Но можно вручную
sudo rm -rf /tmp/*

# НЕ трогайте /var! Там системные данные!
```

---

## 📊 ДИАГНОСТИКА И МОНИТОРИНГ

### Место на диске

```bash
# По разделам
df -h

# Размер папок
du -sh /path

# Что занимает место (система)
du -sh /* | sort -rh

# Самые большие файлы в home
find ~ -type f -size +100M -exec ls -lh {} \;
```

### Время загрузки

```bash
# Общее время загрузки
systemd-analyze

# Медленные сервисы (топ 10)
systemd-analyze blame | head -10

# Граф зависимостей
systemd-analyze critical-chain
```

### Логи ошибок

```bash
# Ошибки за последний час
sudo journalctl -p err --since="1 hour ago"

# Логи конкретного сервиса
sudo journalctl -u service -n 50

# Real-time мониторинг логов
sudo journalctl -f
```

### Процессы и память

```bash
# Список процессов
ps aux

# Интерактивный мониторинг
top
# или (если установлен)
htop

# Память
free -h

# Виртуальная память
vmstat 1 5
```

---

## 🔧 СЕРВИСЫ И SYSTEMD

### Управление сервисами

```bash
# Все включенные при загрузке
systemctl list-unit-files --state=enabled

# Статус сервиса
sudo systemctl status service

# Запустить
sudo systemctl start service

# Остановить
sudo systemctl stop service

# Перезагрузить
sudo systemctl restart service

# Включить при загрузке
sudo systemctl enable service

# Отключить при загрузке
sudo systemctl disable service
```

### Логи сервиса

```bash
# Последние 50 строк
sudo journalctl -u service -n 50

# Real-time
sudo journalctl -u service -f

# За последний час
sudo journalctl -u service --since="1 hour ago"
```

---

## 💾 РЕЗЕРВНЫЕ КОПИИ (если используете Btrfs)

### Снимки (snapshots)

```bash
# Создать снимок перед обновлением
sudo btrfs subvolume snapshot / /.snapshots/backup-$(date +%Y%m%d-%H%M)

# Список снимков
sudo btrfs subvolume list /

# Удалить снимок
sudo btrfs subvolume delete /.snapshots/backup-2026-01-01
```

---

## ⚠️ ЧТО НИКОГДА НЕ ДЕЛАЙТЕ

```bash
❌ rm -rf /              # удалит ВСЮ систему
❌ rm -rf /boot          # система не загрузится
❌ rm -rf /var           # потеряете логи и данные
❌ sudo pacman -S pkg    # БЕЗ -yu = конфликты
❌ dd if=/dev/urandom of=/dev/sda    # перезапишет диск
❌ sudo journalctl --vacuum=0         # потеряете все логи
❌ sudo pacman -Scc      # потом нельзя откатить
❌ Отключать все сервисы подряд      # система не загрузится
```

---

## 📋 ШПАРГАЛКА ОБСЛУЖИВАНИЯ

```bash
# Обновление
sudo pacman -Syu             # раз в неделю!

# Очистка
sudo pacman -Sc              # старые версии кэша
sudo pacman -Rns $(pacman -Qdtq)  # orphan пакеты
sudo journalctl --vacuum-time=3d  # логи старше 3 дней

# Диагностика
df -h                        # место на диске
du -sh /* | sort -rh         # что занимает место
systemd-analyze              # время загрузки
journalctl -f                # логи в real-time

# Сервисы
sudo systemctl enable service   # при загрузке
sudo systemctl start service    # запустить сейчас
systemctl list-unit-files --state=enabled  # какие включены
```

---

## 🔗 ДАЛЬШЕ

[Решение проблем Arch Linux](./05-troubleshooting.md)
