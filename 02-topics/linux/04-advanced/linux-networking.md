---
created: 2026-01-04
updated: 2026-01-04
tags: [linux, networking, tcp-ip, dns, firewall, net-tools, reference]
type: reference
---

# Сетевое администрирование Linux (Networking)

## Основная идея

**Linux - это сетевая операционная система.** Администратор должен понимать как работает сеть, как настроить интерфейсы, как диагностировать проблемы.

**Три главных слоя:**
- **Канальный уровень (Layer 2):** MAC адреса, коммутаторы, ARP
- **Сетевой уровень (Layer 3):** IP адреса, маршрутизация, ICMP
- **Транспортный уровень (Layer 4):** TCP/UDP, порты, соединения

**Почему это критично:**
- Подключение к интернету (DHCP vs static IP)
- Диагностика проблем (ping, traceroute, ss, netstat)
- Firewall и безопасность (iptables, ufw)
- DNS и разрешение имён
- VPN, SSH, удалённое администрирование
- Микросервисы и контейнеры (всё через сеть)

---

## ЧАСТЬ 1: IP адреса и интерфейсы

### Концепция IP адреса

```
IPv4: 192.168.1.100
      ^   ^   ^ ^
      |   |   | └─ хост
      |   |   └─── подсеть C
      |   └─────── подсеть B
      └─────────── подсеть A

Маска подсети (Netmask): 255.255.255.0
                         ^^^^^^^^^^^^^^^ = 24 бита для сети
                                     ^ = 8 битов для хостов

CIDR нотация: 192.168.1.100/24
                          ^^ = 24 бита сети = /24
```

### Типичные приватные диапазоны

```
10.0.0.0/8           = 10.0.0.0 - 10.255.255.255 (большие сети)
172.16.0.0/12        = 172.16.0.0 - 172.31.255.255 (средние)
192.168.0.0/16       = 192.168.0.0 - 192.168.255.255 (малые, home)

Специальные:
127.0.0.1            = localhost (это я сам!)
0.0.0.0              = все интерфейсы
255.255.255.255      = broadcast в локальную сеть
```

### Просмотр интерфейсов

```bash
# Современный способ (ВСЕ дистры)
ip addr show
# или
ip a

# Вывод:
# 1: lo: <LOOPBACK,UP,LOWER_UP> mtu 65536
#     inet 127.0.0.1/8 scope host lo
# 2: eth0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500
#     inet 192.168.1.100/24 brd 192.168.1.255 scope global eth0
#     inet6 fe80::a00:27ff:fe4e:66a1/64 scope link

# Что означает:
# UP = интерфейс включен
# RUNNING = активен и получает трафик
# BROADCAST = может отправлять broadcast
# MULTICAST = поддерживает multicast
# inet = IPv4 адрес
# inet6 = IPv6 адрес

# Только информация о конкретном интерфейсе
ip addr show eth0

# Посмотреть MAC адрес
ip link show eth0
# link/ether 08:00:27:4e:66:a1 brd ff:ff:ff:ff:ff:ff
#        ^ MAC адрес
```

### Временная настройка IP адреса (без сохранения)

```bash
# Посмотреть текущий IP
ip addr show eth0

# Установить IP адрес (временно, потеряется при перезагрузке)
sudo ip addr add 192.168.1.100/24 dev eth0

# Удалить IP адрес
sudo ip addr del 192.168.1.100/24 dev eth0

# Включить интерфейс
sudo ip link set eth0 up

# Отключить интерфейс
sudo ip link set eth0 down
```

---

## ЧАСТЬ 2: Постоянная настройка сети (по дистрибутивам)

### ⭐ Arch Linux (systemd-networkd)

**Место файлов:**
```bash
/etc/systemd/network/
```

**Пример: Static IP**
```ini
# /etc/systemd/network/20-wired.network

[Match]
Name=eth0

[Network]
Address=192.168.1.100/24
Gateway=192.168.1.1
DNS=8.8.8.8
DNS=8.8.4.4
```

**Пример: DHCP (автоматический IP)**
```ini
# /etc/systemd/network/20-wired.network

[Match]
Name=eth0

[Network]
DHCP=yes
```

**Применить конфиги:**
```bash
# Перезагрузить systemd-networkd
sudo systemctl restart systemd-networkd

# Проверить статус
sudo systemctl status systemd-networkd

# Посмотреть IP
ip addr show eth0
```

### Ubuntu/Debian (Netplan)

**Место файлов:**
```bash
/etc/netplan/
```

**Пример: Static IP**
```yaml
# /etc/netplan/01-netcfg.yaml

network:
  version: 2
  ethernets:
    eth0:
      addresses:
        - 192.168.1.100/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

**Пример: DHCP**
```yaml
# /etc/netplan/01-netcfg.yaml

network:
  version: 2
  ethernets:
    eth0:
      dhcp4: true
```

**Применить:**
```bash
sudo netplan apply

# Проверить
ip addr show eth0
```

### Старый способ (Debian/Ubuntu, может быть используется)

**Место файлов:**
```bash
/etc/network/interfaces
```

**Пример: DHCP**
```bash
auto eth0
iface eth0 inet dhcp
```

**Пример: Static IP**
```bash
auto eth0
iface eth0 inet static
  address 192.168.1.100
  netmask 255.255.255.0
  gateway 192.168.1.1
  dns-nameservers 8.8.8.8 8.8.4.4
```

**Применить:**
```bash
sudo systemctl restart networking
```

---

## ЧАСТЬ 3: Маршрутизация и шлюзы

### Просмотр таблицы маршрутов (ВСЕ дистры)

```bash
# Современный способ
ip route show

# Вывод:
# default via 192.168.1.1 dev eth0
# 192.168.1.0/24 dev eth0 proto kernel scope link src 192.168.1.100

# Что означает:
# default = маршрут по умолчанию (неизвестные IP адреса идут сюда)
# via 192.168.1.1 = через этот шлюз (gateway)
# dev eth0 = через этот интерфейс
# 192.168.1.0/24 = прямая доставка в локальную сеть (не нужен gateway)

# Старый способ (может работать, но не везде)
route -n
netstat -rn  # ещё более старый
```

### Добавление маршрутов (временно)

```bash
# Добавить маршрут
sudo ip route add 192.168.2.0/24 via 192.168.1.10 dev eth0

# Добавить маршрут по умолчанию
sudo ip route add default via 192.168.1.1

# Удалить маршрут
sudo ip route del 192.168.2.0/24 via 192.168.1.10 dev eth0

# Заменить маршрут
sudo ip route replace 192.168.2.0/24 via 192.168.1.20 dev eth0
```

---

## ЧАСТЬ 4: DNS и разрешение имён

### Концепция DNS

```
Ты: ping google.com
  ↓
DNS сервер: "google.com это 142.250.185.46"
  ↓
Ты: отправляю пакет на 142.250.185.46
  ↓
Google отвечает
```

### Arch Linux (systemd-resolved)

**Проверить статус:**
```bash
# Какие DNS сервера используются
systemctl status systemd-resolved

# Подробнее
resolvectl status
# или
systemd-resolve --status
```

**Настроить DNS:**
```ini
# /etc/systemd/resolved.conf

[Resolve]
DNS=8.8.8.8 8.8.4.4
FallbackDNS=1.1.1.1
```

**Применить:**
```bash
sudo systemctl restart systemd-resolved

# Проверить
systemd-resolve --status
```

### Ubuntu/Debian (через Netplan)

```yaml
# /etc/netplan/01-netcfg.yaml

network:
  version: 2
  ethernets:
    eth0:
      dhcp4: true
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

```bash
sudo netplan apply
```

### Проверка DNS (ВСЕ дистры)

```bash
# Разрешить имя в IP
nslookup google.com
# Address: 142.250.185.46

# Более подробный способ
dig google.com
# ANSWER SECTION:
# google.com.  300 IN A 142.250.185.46

# Обратное разрешение (IP -> имя)
nslookup 142.250.185.46

# Использовать конкретный DNS сервер
nslookup google.com 8.8.8.8
```

---

## ЧАСТЬ 5: Диагностика сетевых проблем

### ping (проверка связи)

```bash
# Отправить 4 пакета на google.com
ping -c 4 google.com
# PING google.com (142.250.185.46) 56(84) bytes of data.
# 64 bytes from ... time=35.5 ms
# --- google.com statistics ---
# 4 packets transmitted, 4 received, 0% packet loss, time 3004ms
# rtt min/avg/max/stddev = 30.1/35.2/42.1/5.1 ms

# Что означает:
# 0% packet loss = всё пакеты дошли (хорошо!)
# time=35.5 ms = задержка 35.5 миллисекунд (хорошо - менее 100ms)
# rtt = round trip time (туда-обратно)

# Ping localhost (проверка что система работает)
ping localhost
# PING localhost (127.0.0.1) ...
# 0% packet loss = система работает
```

### traceroute (путь до хоста)

```bash
# Показать всех промежуточных маршрутизаторов
traceroute google.com
# 1 192.168.1.1 (192.168.1.1) 2.5 ms
# 2 10.0.0.1 (10.0.0.1) 10.5 ms
# 3 203.0.113.1 (203.0.113.1) 25.3 ms
# ... (остальные маршрутизаторы)
# N 142.250.185.46 (142.250.185.46) 35.5 ms

# Что означает:
# Пакет прошёл через 192.168.1.1 (шлюз дома)
# Потом через 10.0.0.1 (провайдер)
# Потом через маршрутизаторы интернета
# И добрался до google.com
```

### ss и netstat (состояние соединений)

```bash
# Современный способ (ss) - рекомендуется
ss -tlnp
# LISTEN состояние соединений
# t = TCP
# l = listening (слушающие)
# n = числовой формат (без разрешения имён)
# p = показать процесс (требует sudo)

# Вывод:
# State Recv-Q Send-Q Local Address Foreign Address Process
# LISTEN 0 128 0.0.0.0:22 0.0.0.0:* sshd

# Какие соединения активны (не только listening)
ss -tnp

# Статистика по портам
ss -s
# TCP: 10 established, 2 closed, 1 timeWait

# Старый способ (netstat, может быть не установлен на Arch)
sudo netstat -tlnp
# (может потребоваться: pacman -S net-tools)
```

### Какой процесс использует порт

```bash
# Установить net-tools если нет (для Arch)
sudo pacman -S net-tools

# Какой процесс слушает на портах
sudo netstat -tlnp | grep LISTEN
# tcp 0 0 0.0.0.0:22 0.0.0.0:* LISTEN 500/sshd

# Какой процесс использует порт 8080 (ss, лучше)
sudo ss -tlnp | grep 8080
# LISTEN 0 128 0.0.0.0:8080 0.0.0.0:* users:(("my-app",pid=5000))
```

---

## ЧАСТЬ 6: Firewall (ufw и iptables)

### Концепция firewall

```
Входящий трафик:
  Firewall: "Разрешить порт 22 (SSH)?"
  YES → пакет проходит
  NO → пакет блокируется

Исходящий трафик:
  Firewall: "Разрешить отправить на 8.8.8.8:53 (DNS)?"
  YES → пакет отправляется
  NO → пакет блокируется
```

### UFW (простой firewall, для Arch/Ubuntu/Debian)

```bash
# Установить UFW
# Arch:
sudo pacman -S ufw

# Ubuntu/Debian:
sudo apt install ufw

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status

# Разрешить входящий трафик на порт 22 (SSH)
sudo ufw allow 22/tcp
# или
sudo ufw allow ssh

# Разрешить на порт 80 (HTTP)
sudo ufw allow 80/tcp
# или
sudo ufw allow http

# Разрешить на порт 443 (HTTPS)
sudo ufw allow 443/tcp
# или
sudo ufw allow https

# Разрешить с конкретного IP адреса
sudo ufw allow from 192.168.1.100 to any port 22

# Запретить порт
sudo ufw deny 3306/tcp

# Удалить правило
sudo ufw delete allow 22/tcp

# Просмотреть все правила
sudo ufw show added

# Отключить firewall
sudo ufw disable
```

### iptables (продвинутый firewall, сложнее)

```bash
# ⚠️ ОСТОРОЖНО: ошибка = потеря доступа!

# Посмотреть текущие правила
sudo iptables -L -v -n

# Добавить правило (разрешить SSH)
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT

# Добавить правило для HTTP
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT

# Установить политику по умолчанию (DROP = отклонять всё)
sudo iptables -P INPUT DROP

# Сохранить правила (чтобы выжили после перезагрузки)
# Arch:
sudo pacman -S iptables
sudo iptables-save > /etc/iptables/iptables.rules
sudo systemctl enable iptables
sudo systemctl start iptables

# Ubuntu/Debian:
sudo apt install iptables-persistent
sudo iptables-save > /etc/iptables/rules.v4
```

---

## ЧАСТЬ 7: SSH и удалённый доступ

### Концепция SSH

```bash
# SSH = Secure Shell = защищённый доступ по сети
ssh user@192.168.1.100
# Подключиться к удалённой машине как пользователь user

ssh -i ~/.ssh/id_rsa user@example.com
# Подключиться с использованием SSH ключа (более безопасно)
```

### SSH клиент (ВСЕ дистры)

```bash
# Подключиться к серверу
ssh user@192.168.1.100

# С портом (если не 22)
ssh -p 2222 user@192.168.1.100

# С SSH ключом
ssh -i ~/.ssh/id_rsa user@example.com

# С verbose (показать детали подключения)
ssh -v user@example.com

# Выполнить команду и выйти
ssh user@example.com "ls -la /home/user"

# Туннель (port forwarding)
ssh -L 8000:localhost:8000 user@example.com
# Локально на 8000 → удалённо localhost:8000
```

### SSH сервер (ВСЕ дистры)

```bash
# Установить SSH сервер
# Arch:
sudo pacman -S openssh

# Ubuntu/Debian:
sudo apt install openssh-server

# Запустить SSH демон
sudo systemctl enable sshd
sudo systemctl start sshd

# Убедиться что слушает на порту 22
sudo ss -tlnp | grep :22
# LISTEN 0 128 0.0.0.0:22

# Конфигурация SSH
sudo nano /etc/ssh/sshd_config

# Важные параметры:
# Port 22 = слушать на порту 22
# PermitRootLogin no = не разрешать логин как root (безопаснее)
# PubkeyAuthentication yes = разрешить логин по ключам
# PasswordAuthentication yes = разрешить логин по пароли

# После изменения конфига:
sudo systemctl restart sshd
```

---

## ЧАСТЬ 8: TCP vs UDP, порты и сокеты

### Концепция портов

```
IP адрес = адрес машины (192.168.1.100)
Порт = адрес приложения на машине (22, 80, 443)

Полный адрес сокета: 192.168.1.100:22
                     ^IP адрес^ ^порт^

Специальные порты:
0-1023 = системные (только root может слушать)
1024-49151 = пользовательские (зарегистрированные)
49152-65535 = эфемерные (для исходящих соединений)

Типичные:
22 = SSH (secure shell)
53 = DNS
80 = HTTP (веб)
443 = HTTPS (веб безопасный)
3306 = MySQL
5432 = PostgreSQL
6379 = Redis
8000-9000 = разработка (приложения)
```

### TCP vs UDP

```bash
# TCP (надёжный, в порядке):
# - SSH, HTTP, HTTPS, FTP, SMTP, MySQL, PostgreSQL
# - Гарантирует доставку и порядок
# - Медленнее, но надежнее

# UDP (быстрый, может потерять):
# - DNS, NTP, VoIP, видеотрансляция
# - Может потерять пакеты
# - Быстро

# Посмотреть TCP соединения
ss -tnp

# Посмотреть UDP соединения
ss -unp

# Посмотреть оба
ss -tunp
```

---

## ЧАСТЬ 9: Практический сценарий - Безопасный веб сервер на Arch

### Полная настройка

**Шаг 1: Настроить сеть (systemd-networkd)**
```ini
# /etc/systemd/network/20-wired.network

[Match]
Name=eth0

[Network]
Address=192.168.1.100/24
Gateway=192.168.1.1
DNS=8.8.8.8
DNS=8.8.4.4
```

```bash
sudo systemctl restart systemd-networkd
```

**Шаг 2: Включить firewall (ufw)**
```bash
sudo pacman -S ufw
sudo ufw enable
sudo ufw default deny incoming
sudo ufw default allow outgoing

# Разрешить SSH, HTTP, HTTPS
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

sudo ufw status
```

**Шаг 3: Настроить SSH (опционально)**
```bash
sudo nano /etc/ssh/sshd_config
# PermitRootLogin no
# PasswordAuthentication no (если используешь ключи)

sudo systemctl restart sshd
```

**Шаг 4: Диагностика**
```bash
# Проверить IP
ip addr show

# Проверить маршруты
ip route show

# Проверить DNS
systemd-resolve --status

# Проверить интернет
ping 8.8.8.8

# Проверить что веб сервер слушает
sudo ss -tlnp | grep 80
```

---

## ЧАСТЬ 10: Шпаргалка (быстрая справка)

### IP адреса и интерфейсы (ВСЕ)

```bash
ip addr show              # Посмотреть адреса
ip link show              # Посмотреть интерфейсы
sudo ip addr add IP/MASK dev ETH0   # Добавить адрес (временно)
sudo ip link set ETH0 up  # Включить интерфейс
```

### Конфигурация сети

```bash
# Arch (systemd-networkd):
sudo nano /etc/systemd/network/20-wired.network
sudo systemctl restart systemd-networkd

# Ubuntu/Debian (netplan):
sudo nano /etc/netplan/01-netcfg.yaml
sudo netplan apply
```

### Маршруты (ВСЕ)

```bash
ip route show             # Таблица маршрутов
sudo ip route add ... via ...  # Добавить маршрут
```

### DNS (ВСЕ)

```bash
# Arch:
systemd-resolve --status

# Ubuntu/Debian:
cat /etc/resolv.conf

# Тест (ВСЕ):
nslookup google.com
dig google.com
```

### Диагностика (ВСЕ)

```bash
ping HOST                 # Проверка связи
traceroute HOST           # Путь до хоста
ss -tlnp                  # Активные соединения
sudo netstat -tlnp        # (старый способ, может не быть)
```

### Firewall (ВСЕ)

```bash
sudo ufw enable           # Включить
sudo ufw allow PORT       # Разрешить порт
sudo ufw deny PORT        # Запретить порт
sudo ufw status           # Статус
```

### SSH (ВСЕ)

```bash
ssh user@HOST             # Подключиться
ssh -i KEY user@HOST      # С ключом
ssh -p PORT user@HOST     # На другом порту
ssh -L LOCAL:REMOTE host  # Туннель
```

---

## Связанные заметки

### ← Перед этим (предусловие)
- [[linux-system-basics]] - основы Linux (всё начинается отсюда)
- [[systemd-basics]] - systemd для сетевых сервисов

### ↔ Параллельно (рядом)
- [[linux-system-hardening]] - firewall это часть безопасности
- [[systemd-guide-extended]] - systemd для сетевых сервисов

### 🔒 Применение (в этом файле)
- SSH (защищённый доступ)
- Firewall (ufw, iptables)
- Диагностика проблем (ping, traceroute, ss)

### 📚 Главный индекс
- [[00-start-here-index]] - полная навигация по базе знаний

---

## Источники

- `man ip` - полная документация ip команды (ВСЕ)
- `man ss` - современный способ просмотра сокетов (ВСЕ)
- `man systemd-networkd` - Arch конфигурация (Arch)
- `man netplan` - Ubuntu/Debian конфигурация (Ubuntu/Debian)
- `man ufw` - простой firewall (ВСЕ)
- `man iptables` - продвинутый firewall (ВСЕ)
- `man ssh` - SSH клиент (ВСЕ)
- Linux man-pages: tcp(7), udp(7), socket(7)
- Arch Wiki: Network configuration
- Arch Wiki: Firewall
- Ubuntu Manuals: Netplan

---

Создано: 2026-01-04
